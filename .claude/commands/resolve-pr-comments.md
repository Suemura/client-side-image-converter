Github Pull Requestのレビューコメントを読み取り、指摘への対応・質問への回答・必要な修正を行ってください。

PR: $ARGUMENTS

## 手順

1. `gh pr view $ARGUMENTS` でPRの概要を確認
2. `gh pr diff $ARGUMENTS` でdiffを取得し、変更内容を把握
3. PRのレビューコメントを取得:
   ```bash
   gh api repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}/comments
   gh api repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}/reviews
   ```
4. 各コメントについて以下を判断・実行:

### コメント種別ごとの対応

#### 修正指摘の場合

- 指摘が**妥当**か判断する（コードの文脈、プロジェクトの規約、技術的正当性を考慮）
- **妥当な場合**: コードを修正し、コメントに対応した旨を返信
- **妥当でない場合**: 理由を説明する返信を投稿（コードは変更しない）

#### 質問の場合

- コードの実装意図や背景を読み取り、的確に回答を返信

#### 既に解決済み・対応不要の場合

- その旨を簡潔に返信

## コメントへの返信方法

```bash
gh api repos/{OWNER}/{REPO}/pulls/{PR_NUMBER}/comments/{COMMENT_ID}/replies \
  -f body="返信内容"
```

## 返信のフォーマット

返信の冒頭には必ず以下の一文を入れてください:

> この返信は **Claude Code (AI)** によるものです。

その後、以下のプレフィックスを使い分けてください:

- **[修正済み]** コードを修正した場合（修正内容の簡潔な説明を含める）
- **[対応不要]** 指摘が妥当でないと判断した場合（理由を明記）
- **[回答]** 質問への回答
- **[確認]** 判断に迷う場合（ユーザーに確認を求める）

## 修正時の注意事項

- 修正はレビュー指摘の範囲に留め、関係のないコード変更は行わない
- 修正後は `npm run lint:fix` を実行してコードを整える
- 型エラーが懸念される場合は `npm run typecheck` も実行する
- `src/utils/` を修正した場合は `npm run test` でテストが通ることを確認する
- 修正をコミット・プッシュしてPRに反映する

## 出力

最後に対応結果のサマリーを表示してください:

- コメント総数
- 対応内訳（修正済み / 対応不要 / 回答 / 確認）
- 修正したファイルの一覧（修正があった場合）
