# CLAUDE.md

このファイルは、このリポジトリでコードを扱う際の Claude Code (claude.ai/code) へのガイダンスを提供します。

## プロジェクト概要

Next.js App Router で構築されたプライバシー重視のクライアントサイド画像変換ウェブアプリケーション。すべての画像処理はブラウザ内で実行され、画像がサーバーに送信されることはありません。

## 開発コマンド

```bash
# 依存関係のインストール（npm を使用。lockfile は package-lock.json）
npm install

# Turbopack による開発サーバーの起動
npm run dev

# リンティングの実行
npm run lint

# リント違反の自動修正 + フォーマット
npm run lint:fix

# 型チェックの実行
npm run typecheck

# 単体テストの実行
npm run test

# 単体テストのウォッチモード
npm run test:watch

# E2E テストの実行（Playwright / build + out/ の静的配信をポート 3100 で自動起動）
npm run e2e

# E2E テストの UI モード
npm run e2e:ui

# 本番用ビルド（静的エクスポート。webpack ビルド、下記注記参照）
# postbuild で sitemap（next-sitemap）と Service Worker（scripts/generate-sw.ts → out/sw.js）を続けて生成する
npm run build

# Cloudflare Pages へのデプロイ
npm run deploy

# デプロイ版のローカルプレビュー
npm run preview
```

※ Next.js 16 はデフォルトで Turbopack を本番ビルドに使うが、本プロジェクトでは「Creating an optimized production build ...」で無限ハングする上流の既知問題（vercel/next.js discussion #77102 と同症状）があるため、`build` スクリプトは `next build --webpack` にオプトアウトしている。dev サーバー（`next dev --turbopack`）は正常動作するため Turbopack のまま。**上流修正後に Turbopack ビルドへ戻す暫定対応**。

## 完了条件（Definition of Done）

コード変更を伴うタスクは、以下がすべて成功した状態で完了とすること：

1. `npm run lint` — Biome によるリント・フォーマットチェック
2. `npm run typecheck` — TypeScript 型チェック
3. `npm run test` — vitest による単体テスト

これらが失敗したまま作業を終了しないこと。なお、応答終了時（Stop フック）に TS/TSX ファイルの変更があると上記 3 つが自動実行され、失敗した場合はエラー内容が差し戻される。

## アーキテクチャ

### 技術スタック
- **フレームワーク**: Next.js 16 with App Router
- **言語**: TypeScript（strict モード）
- **スタイリング**: CSS Modules
- **i18n**: react-i18next（日本語/英語サポート）
- **リンター/フォーマッター**: Biome（ESLint/Prettier の代替）
- **テスト**: vitest + happy-dom
- **デプロイ**: Cloudflare Pages（静的エクスポート）

### 主要ディレクトリ構造
- `src/app/` - Next.js App Router ページ
  - `convert/` - 画像フォーマット変換ページ
  - `crop/` - 画像トリミングツールページ（`components/CropSelector.tsx`（8方向ハンドル操作・比率固定リサイズ）と `components/CropToolbar.tsx`（アスペクト比・回転/反転・適用範囲）で構成）
  - `edit/` - 画像編集ページ（写真アプリ相当のライト/カラー/ディテール/効果の調整 + 自動補正（WB スポイト含む）+ トーンカーブ + LUT フィルタ + ヒストグラム表示、Issue #66 / #67 / #68 の第 1〜6 項目。`components/AdjustmentPanel.tsx`（先頭に自動補正グループ = オートレベル / 自動ホワイトバランスのワンショットチップボタン + WB スポイトのモードトグル（`aria-pressed`）、続いてライト/カラー/ディテール/効果のスライダー群（モノクロは 0/100 のチェックボックストグル、シャープネス / グレインは `ADJUSTMENT_UI_MIN` による 0 起点の片方向スライダー）。props `onAutoLevels` / `onAutoWhiteBalance` / `onToggleEyedropper` / `eyedropperActive` / `autoDisabled`）・`components/EditToolbar.tsx`（適用範囲・出力形式・EXIF 保持・全体リセット）・`components/CompareView.tsx`（分割 Before/After 比較・画像ナビ・WebGL レンダラ保持。編集後プレビューの描画完了ごとに縮小サンプリングした ImageData を渡す optional prop `onEditedFrame`、WB スポイト用の optional props `eyedropperActive` / `onEyedropperPick`（モード中は分割ドラッグの代わりにクリック点をソース自然座標で親へ渡す。crosshair カーソル + ヒント文切替）付き）・`components/ToneCurvePanel.tsx`（SVG トーンカーブエディタ。RGB マスター / 輝度のチャンネル切替チップ・クリックで点追加 → そのままドラッグ配置・ドラッグ移動・ダブルクリック削除（点の追加・ドラッグはプライマリポインタの左ボタンのみ）・編集前（カーブ適用前）の輝度ヒストグラム背景・チャンネル単位のカーブリセット。数式・点操作は `toneCurve.ts` へ委譲し座標変換とイベント処理に徹する）・`components/LutPicker.tsx`（LUT プリセットサムネイル一覧・「なし」・カスタムアップロード（.cube / HALD PNG）・適用強度スライダー・エラー通知）・`components/HistogramPanel.tsx`（調整・トーンカーブ・LUT 適用後のヒストグラムを SVG パスで表示。RGB / 輝度の切替チップ）で構成）
  - `metadata/` - EXIF メタデータエディターページ
  - `manifest.ts` - PWA の Web App Manifest。Next が `/manifest.webmanifest` として静的出力し `<link rel="manifest">` を自動注入する（`dynamic = "force-static"`）
- `src/components/` - 再利用可能な React コンポーネント（PWA 関連: `ServiceWorkerRegister.tsx`（本番ビルドでのみ `/sw.js` を登録・UI なし）/ `InstallPrompt.tsx`（`beforeinstallprompt` を受けた控えめな A2HS 導線）。`Slider.tsx` はラベル・値表示・個別リセット付きの汎用レンジスライダー（編集ページの調整パネルで使用）。ツール連携（ハンドオフ）関連: `HandoffSend.tsx`（結果を次のツールへ送る送出コントロール。`Results.tsx` の optional prop `handoffOrigin` 指定時に表示・送り先候補 0 件なら非描画・マウント時に送り先ルートを `router.prefetch`）/ `HandoffNotice.tsx`（受け取りページの到着バナー。`role="status"`・取り込めなかった件数の補足・閉じるボタン））
- `src/contexts/` - React Context（`ThemeContext.tsx`（ライト/ダークテーマ）/ `HandoffContext.tsx`（`HandoffProvider` / `useHandoff`。ツール連携の共有ストアを配る。ストア本体は Next.js のクライアント遷移でルートレイアウトごと再マウントされても失われないようモジュールスコープのシングルトンで保持し、`usePathname` の変化を `store.onNavigate` へ通知する））
- `src/utils/` - コアユーティリティクラス
  - `imageConverter.ts` - 画像フォーマット変換処理（単体変換 `convertImage` と一括変換 `convertMultipleImages`。一括は対応環境で Worker プールへ委譲し、非対応環境ではメインスレッド逐次処理。`mode: "optimize"` のときは形式維持の最適化として `optimizeImage` に分岐し、`optimizeImage` を再エクスポートする）
  - `conversionCore.ts` - 変換のコア型（`ConversionFormat` / `ConversionMode`（"convert" / "optimize"） / `ConversionOptions`（`mode` を含む） / `ConversionResult` 等）と Canvas 非依存の純粋ロジック（`searchQualityForTargetSize` / `calculateTargetSize`）。メインスレッドと Worker で共有（`imageConverter.ts` から再エクスポートし既存 import 経路を維持）
  - `conversionResult.ts` - `ConversionResult` 組み立ての共通化（変換用 `buildConversionResult` / 最適化用 `buildOptimizeResult`（同一形式のため元ファイル名を維持）/ 編集用 `buildEditResult`（`_edited` サフィックス + 出力形式の拡張子。`originalFilename` は元名保持））。convertImage・ワーカープール・imageEditor で共用
  - `concurrency.ts` - 純粋な並行スケジューラ（`mapWithConcurrency` / `resolveConcurrency`）。同時実行数制限・入力順保持・continue-on-error・逐次進捗
  - `pngQuality.ts` - PNG 品質ティア判定（`pngQualityStrategy`。閾値 95/70）。メインスレッドと Worker で共有
  - `decodedImage.ts` - HEIC/TIFF デコード結果（RGBA 生ピクセル）の共有型 `DecodedImage`
  - `avifEncoder.ts` - AVIF エンコード処理（`@jsquash/avif` の WASM を動的 import。`HTMLCanvasElement | OffscreenCanvas` 両対応）
  - `optimizeCore.ts` - 画像最適化（形式維持の再圧縮）のコア型と Canvas / WASM / DOM 非依存の純粋ロジック（型 `OptimizeEngine` / `WebpEncoding`、形式→エンジンをディスパッチする `resolveOptimizeEngine` / `isOptimizableType`、no-worse-than-original 判定の `pickSmallerSize`、RIFF/VP8L/VP8/VP8X 走査で WebP のロスレス/ロッシーを判定する `detectWebpEncoding`）。メインスレッドと Worker で共有し単体テスト対象（`conversionCore.ts` と同方針）
  - `imageOptimizer.ts` - 形式維持の最適化のブラウザ側オーケストレーション（`@jsquash/oxipng` / `@jsquash/jpeg` / `@jsquash/webp` を動的 import。Canvas 非依存でメイン/Worker 共用の `optimizeImageBuffer`、メインスレッド用ラッパー `optimizeImage`）
  - `heicDecoder.ts` - HEIC/HEIF デコード処理（libheif WASM、動的 import。Canvas 非依存の `decodeHeicToImageData` を含む）
  - `tiffDecoder.ts` - TIFF デコード処理（`utif2` の純 JS デコーダー、動的 import。Canvas 非依存の `decodeTiffToImageData` を含む）
  - `imageCropper.ts` - 画像トリミング処理（EXIF Orientation 補正 + 回転/反転をキャンバスに焼き込む `renderOrientedImage` / `createOrientedPreviewUrl`、`cropImage`（`transform` / `quality` 引数対応）、画像ごとの領域・変換を受け取る `cropImages`（`CropJob[]`）。`CropArea` / `CropTransform` は `cropGeometry.ts` から再エクスポート）
  - `cropGeometry.ts` - トリミングの座標計算に関する Canvas 非依存の純粋ロジック（型 `CropArea` / `CropTransform` / `CropState`、`ASPECT_RATIO_PRESETS`、`clampCropArea` / `scaleCropArea` / `toDisplayArea` / `fitAspectRatio` / `enforceAspectRatio` / `orientedSize` / `rotateRight` / `rotateLeft` / `resolveCropForIndex`）。CropSelector（表示座標）・crop ページ（自然座標保持）・imageCropper（出力）で共有し単体テスト対象
  - `adjustments.ts` - 画像編集の調整（ライト/カラー/ディテール/効果）のコア型と Canvas / WebGL 非依存の純粋ロジック（型 `AdjustmentState` / `EditState`、`DEFAULT_ADJUSTMENTS`、`LIGHT_ADJUSTMENT_KEYS` / `COLOR_ADJUSTMENT_KEYS` / `DETAIL_ADJUSTMENT_KEYS` / `EFFECT_ADJUSTMENT_KEYS` / `ADJUSTMENT_KEYS`、UI スライダー下限の例外 `ADJUSTMENT_UI_MIN`（sharpness / grain は 0 起点の片方向）、`clampAdjustments` / `normalizeAdjustments` / `isDefaultAdjustments`、GPU/CPU 共通の色調整数式の唯一の真実 `applyAdjustmentToPixel`（sRGB 空間・固定パイプライン順・Rec.709 luma。手順 1b にガンマ = 露光直後（c ≥ 0 が保証される唯一の位置）の冪変換 γ = 2^(-n)、手順 9b にモノクロ = 色相の後（調整の最後）の luma 化 0/100 トグルを含む）、自動補正の逆算用に輸出するパイプライン係数 `blacksToneWeight` / `whitesToneWeight` / `TEMPERATURE_SHIFT` / `TINT_SHIFT`（`applyAdjustmentToPixel` のインラインリテラルを関数/定数参照へ置換したもので数式・挙動は不変。GLSL 側はリテラルのままミラー）、`effects.ts` と共有する GLSL 互換スカラー `clamp01` / `smoothstep`、`resolveCropForIndex` を踏襲した `resolveAdjustmentForIndex`）。単体テスト対象
  - `effects.ts` - ディテール（シャープネス / 明瞭度）とビネット / グレインの Canvas / WebGL / DOM 非依存な純粋ロジック（per-pixel 色変換だけの `adjustments.ts` と異なり、近傍参照（ディテール = 輝度の unsharp mask）と画素位置（ビネット = 中心からの距離、グレイン = 座標ハッシュ）に依存する効果。係数定数 `SHARPNESS_GAIN` / `CLARITY_GAIN` / `VIGNETTE_STRENGTH` / `VIGNETTE_INNER` / `GRAIN_STRENGTH`、3×3 ガウスカーネル (1,2,1)⊗(1,2,1)/16 の単一の真実 `GAUSS3_TAPS`（GLSL のタップ行はビルダーがここから生成。y 対称のためテクスチャの Y 反転に影響されない）、明瞭度の解像度適応整数ストライド `clarityStride`（短辺 200px ごと・下限 2）、CPU パス用の輝度平面前処理 `computeLumaPlane`（Float32Array を 1 回構築）/ `blurLumaAt` / 中間調マスク `midtoneWeight` / 輝度 unsharp mask の `detailDeltaAt`、周辺減光の `vignetteFactorAt`（画素中心の対角正規化距離 + smoothstep(0.3, 1.0)。負値で増光・非正方形はアスペクト追従の楕円）、lowbias32 整数ハッシュによる決定的グレインの `lowbias32`（`LOWBIAS32_M1/M2`）/ `hashPixel` / `grainNoiseAt`（`Math.imul` の 32bit 演算で GLSL の uint 演算とビット同一・上位 24bit のみ float 化して GPU(fp32)/CPU(fp64) の粒を一致させる。`Math.random` 不使用でプレビューと出力も同じ粒））。単体テスト対象
  - `adjustmentShader.ts` - WebGL2 シェーダ文字列生成の Canvas/WebGL 非依存な純粋ロジック（`buildAdjustmentShader`（GLSL ES 3.00。パイプラインは ディテール（ソース直後。`texelFetch` の近傍タップによる輝度 unsharp mask。タップ行は `effects.ts` の `GAUSS3_TAPS` から生成しマルチパス / FBO 不要の単一シェーダのまま）→ 調整（`applyAdjustmentToPixel` を同順序・同係数でミラー。ガンマ / モノクロ含む）→ トーンカーブ（256×1 テクスチャ。テクセル中心補正 (v*255+0.5)/256 の lookup → 輝度カーブの加算シフト。`applyToneCurveToPixel` を同順序でミラー）→ LUT 色変換フィルタ（`precision highp sampler3D`。ドメイン正規化 → 3D テクスチャのトライリニア lookup → 強度ブレンド。`applyLutToPixel` を同順序でミラー）→ ビネット / グレイン（gl_FragCoord → 画像座標の Y-flip 変換込み。lowbias32 の uint 実装で `vignetteFactorAt` / `grainNoiseAt` をミラー。係数・カーネル・ハッシュ乗数は `effects.ts` の定数を埋め込み）→ 最終クランプ）/ `VERTEX_SHADER_SOURCE` / キー→uniform 名の対応表 `ADJUSTMENT_UNIFORMS`（調整の配線の単一の真実）/ ディテール補助 uniform の対応表 `EFFECT_UNIFORMS`（`u_clarityStride`。ディテールの配線の単一の真実）/ トーンカーブのサンプラー名 `CURVE_SAMPLER` と uniform 名の対応表 `CURVE_UNIFORMS`（トーンカーブの配線の単一の真実）/ LUT サンプラー名 `LUT_SAMPLER` と LUT uniform 名の対応表 `LUT_UNIFORMS`（LUT の配線の単一の真実））。単体テスト対象
  - `lutParser.ts` - LUT（色変換フィルタ）のパースと CPU 適用の Canvas / WebGL / DOM 非依存な純粋ロジック（型 `LutData`（常に正規化済み 3D グリッド）、`.cube`（1D / 3D。`LUT_3D_SIZE` / `LUT_1D_SIZE` / `DOMAIN_MIN` / `DOMAIN_MAX` / `TITLE` 対応）をパースする `parseCubeLut`、HALD CLUT の RGBA を 3D 化する `haldClutToLutData`、恒等 LUT 生成 `createIdentityLut`、GPU の GLSL とミラーする CPU トライリニア + 強度ブレンドの `applyLutToPixel`）。1D LUT は分離可能変換として `LUT_1D_EXPAND_SIZE` を上限に 3D へ厳密展開し lookup 経路を 3D に一本化する。不正フォーマットは Error。単体テスト対象
  - `lutState.ts` - LUT の選択状態と適用範囲解決の Canvas / WebGL / DOM 非依存な純粋ロジック（軽量な選択 `LutSelection`（`lutId` + `strength`。重いデータ本体は edit ページの ref レジストリが保持）、同梱プリセットカタログ `LUT_PRESETS` / カスタム予約 ID `CUSTOM_LUT_ID`、`adjustments.ts` の `resolveAdjustmentForIndex` を踏襲した dual-store 解決 `resolveLutForIndex`）。単体テスト対象
  - `lutLoader.ts` - LUT のブラウザ側読み込みオーケストレーション（プリセットの動的 fetch + モジュール内キャッシュ `loadPresetLut`、ユーザーの `.cube` テキスト読み込み `loadCubeFromFile`、HALD CLUT PNG を Canvas でデコードする `loadHaldFromFile`、拡張子で振り分ける `loadLutFromFile`）。パース・変換は `lutParser.ts` に委譲。Canvas / fetch 依存のため E2E 検証
  - `histogram.ts` - 編集ページのヒストグラム表示のコア型と Canvas / DOM 非依存の純粋ロジック（型 `HistogramData`、`HISTOGRAM_BINS`（256）/ `HISTOGRAM_MAX_SAMPLE_PIXELS`（65536 = 256×256 相当）、RGBA バイト列から 256 ビンの R/G/B/輝度を算出する `computeHistogram`（輝度は `adjustments.ts` の `LUMA_WEIGHTS`（Rec.709）を再利用・alpha=0 の完全透明ピクセルはスキップ）、アスペクト維持でサンプリング寸法を解決する `resolveHistogramSampleSize`、RGB 重畳の共通スケールを求める `histogramMaxCount`、SVG パス文字列を生成する `buildHistogramPath`）。単体テスト対象
  - `toneCurve.ts` - トーンカーブ（RGB マスター / 輝度チャンネル）のコア型と Canvas / WebGL / DOM 非依存の純粋ロジック（型 `CurvePoint` / `CurveChannel`（"rgb" | "luminance"）/ `ToneCurveState` / `ToneCurveEditState`、定数 `CURVE_LUT_SIZE`（256）/ `MAX_CURVE_POINTS`（16）/ `CURVE_POINT_MIN_GAP`（0.01）/ `DEFAULT_TONE_CURVE`、点操作 `addCurvePoint` / `moveCurvePoint` / `removeCurvePoint`（端点は x 固定で y のみ可動・削除不可。拒否時は同一配列参照を返す契約）、Fritsch–Carlson 単調 3 次 Hermite 補間の `evaluateCurve` / `buildCurveLut`、GPU/CPU 共通テーブル焼成 `buildToneCurveTable`（RGBA インターリーブの Float32Array(256×4)。`.rgb`=各チャンネルカーブ（現状マスター同値・将来 R/G/B 個別カーブへ拡張可能）/ `.a`=輝度カーブ。焼成時に 8bit 量子化し GPU の RGBA8 テクセルと CPU の参照値を同一にする）、GPU の LINEAR サンプリングと同式の `sampleCurveTable`（floor+lerp）、CPU 適用 `applyToneCurveToPixel`（RGB マスターカーブ → 輝度カーブの加算シフト。luma は Rec.709 `LUMA_WEIGHTS`）、`isDefaultToneCurve` / dual-store 解決 `resolveToneCurveForIndex`）。単体テスト対象
  - `autoAdjust.ts` - 自動補正（オートレベル / 自動ホワイトバランス / WB スポイト）の Canvas / WebGL / DOM 非依存な純粋ロジック（`AUTO_LEVELS_CLIP_RATIO`（片側 0.5%）、クリップ付きパーセンタイル範囲 `histogramPercentileRange`、輝度ヒストグラムの黒点・白点から blacks / whites スライダー値を逆算する `computeAutoLevels`、`channelMeansFromHistogram`、「無彩色にしたい点の色」から temperature / tint を逆算する WB 逆算式の単一の真実 `computeWhiteBalanceForNeutralPoint`、チャンネル平均を同関数へ渡す特殊形としての gray-world `computeAutoWhiteBalance`、WB スポイトのサンプリング/座標ヘルパー `WB_SAMPLE_RADIUS`（2 = 5×5 窓）/ `clampSampleWindow`（サンプリング窓の境界クランプ。getImageData の読み出しを最大 5×5 に有界化）/ `averageRgb`（RGBA 平均。alpha=0 除外は `computeHistogram` と同基準）/ `displayPointToSourcePixel`（表示座標→ソース自然座標））。編集前の統計（`histogram.ts` の `HistogramData`、または WB スポイトのサンプリング点）だけを入力に既存の調整スライダー値（UI 単位）を返す「統計 → スライダー値」変換に徹し、描画パイプラインは不変（逆算には `adjustments.ts` が輸出するパイプライン係数を使い `applyAdjustmentToPixel` との整合を保つ）。単体テスト対象
  - `webglImageRenderer.ts` - ライト/カラー/ディテール/効果の調整（+ トーンカーブ / LUT）の描画オーケストレーション（永続 WebGL2 レンダラ `createAdjustmentRenderer`（WebGL2 非対応・コンテキスト取得失敗・シェーダコンパイル失敗のいずれでも null を返すため呼び出し側の事前可用性チェックは不要。2D 画像テクスチャ・トーンカーブ 256×1 テクスチャ（TEXTURE2・内部形式 RGBA8。既定は恒等テーブルで常時 complete）・LUT 3D テクスチャ（TEXTURE1・内部形式 RGB8。既定は恒等 LUT で常時 complete）・program 使い回し、明瞭度ストライド `u_clarityStride`（`clarityStride(width, height)`）のアップロード、同一ソースでは調整値のみ変更時にテクスチャ再アップロードを省略・トーンカーブ / LUT もデータ参照が変わったときだけ再アップロード、dispose）/ プレビューと出力の共通経路 `applyAdjustmentsToCanvas`（renderer 有=GPU・null=Canvas2D CPU フォールバックで ディテール（`computeLumaPlane` の前処理 1 回 + `detailDeltaAt`）→ 調整 → `applyToneCurveToPixel` → `applyLutToPixel` → `vignetteFactorAt` / `grainNoiseAt` → 最終クランプ の順に適用。値 0 の効果は前処理・タップとも完全スキップ）/ 型 `LutApplication`（LUT データ + 強度））。Canvas/WebGL 依存のため E2E 検証
  - `imageEditor.ts` - 画像編集のブラウザ側オーケストレーション（型 `EditJob`（`lut?: LutApplication | null` / `curve?: Float32Array | null`（`buildToneCurveTable` の焼成テーブル。null は恒等スキップ）を含む） / `EditOutputFormat` / `EditOptions` / `BatchEditResult`、`renderOrientedImage`→調整・トーンカーブ・LUT 適用→エンコード→EXIF 挿入の `renderEdited`、レンダラを 1 個生成して全ファイルで使い回し finally dispose する `editImages`（失敗一覧を返す））。Canvas/WebGL 依存のため E2E 検証
  - `metadataManager.ts` - EXIF データ管理（JPEG / PNG / WebP からの読み取り、GPS の十進変換・丸めの純粋関数を含む）
  - `exifBinary.ts` - Canvas / WASM / ブラウザ API 非依存の純粋なバイナリ操作群（PNG eXIf チャンク・WebP RIFF/VP8X チャンクの抽出/挿入、CRC32、`Exif\0\0` 識別子の付け外し、piexif dump ↔ 純 TIFF 変換、合成 JPEG 生成）
  - `exifTransfer.ts` - ソース EXIF の読み取りから出力 Blob への書き込みまでを橋渡しするブラウザ側ロジック（`exifWritableFormat` / `readExifTiffFromDataUrl` / `insertExifIntoBlob`、および焼き込み済み出力に合わせて純 TIFF の Orientation を 1 に正規化しつつ実ピクセル寸法タグ（PixelXDimension/PixelYDimension）を出力寸法へ揃える `normalizeExifForBakedImage`）。変換・トリミング・編集の各経路で共用
  - `pageMetadata.ts` - ページ別 SEO メタデータ（title / description / OGP / Twitter / canonical）を組み立てる純粋関数 `buildPageMetadata` とサイト定数（`SITE_NAME` / `SITE_URL` / `SITE_LOCALE`）
  - `directoryReader.ts` - フォルダドロップ時に FileSystem Entry API を再帰走査して配下の File を収集する純粋ロジック（`getEntriesFromDataTransferItems` / `collectFilesFromEntries`）。`collectFilesFromEntries(entries, options?)` は `readEntries` を 1 バッチずつ処理するストリーミング列挙で、`options.maxFiles`（accept 通過ファイル基準の上限）に達したら以降のバッチを読まないため、フラットな巨大フォルダでもメモリに載る Entry ハンドルを「バッチサイズ × 再帰の深さ」に有界化する（巨大ツリー誤投入時のメモリ圧迫を防ぐ）。`options.accept` 述語を通過したファイルだけをバジェットに数え・収集するため、サイドカー（.xmp/.json 等）や空サブフォルダはバジェットを消費しない（生ファイル数ではなく有効ファイル数で打ち切り、深部の画像取りこぼしを防ぐ）。戻り値は `{ files, reachedLimit }` で、`reachedLimit`（上限で走査を打ち切った事実）を呼び出し側の取りこぼし警告に連動させる（maxFiles 未指定時は無制限で常に false）
  - `handoff.ts` - ツール連携（ハンドオフ）のコア型と Canvas / DOM 非依存の純粋ロジック（型 `ToolId` / `HandoffTool` / `HandoffPayload` / `HandoffStore`、全 4 ツールのメタ定義 `HANDOFF_TOOLS`（id / path / labelKey / acceptedTypes / canReceiveHandoff。Navigation とも共有する単一の真実。Phase 1 = Issue #70 で受け取り可能なのは convert / crop のみ）、送り先候補算出 `resolveHandoffTargets`（送り元自身と受け取り未配線ツールを除外し、結果の全 MIME を受理できるツールのみ返す）、結果→File[] 変換 `conversionResultsToFiles` / `cropResultsToFiles`（失敗結果はスキップ・実エンコード結果を反映する `croppedBlob.type` を使用・ZIP ダウンロードと同じ `_2` 連番の同名一意化）、「1 回のナビゲーションだけ生存」するペイロードストア生成 `createHandoffStore`（到着ページの間は冪等に読める＝Next.js の二重マウント耐性・送出元ページには配送しない・到着後に別パス名で consume / onNavigate された時点で破棄＝戻る操作での二重取り込み防止））。単体テスト対象
  - `precache.ts` - Service Worker のプリキャッシュ判定・URL 変換（trailingSlash に合わせ index.html をディレクトリ URL 化）・キャッシュバージョン算出（FNV-1a）・キャッシュ名生成の純粋関数（`shouldPrecache` / `toCacheUrl` / `buildPrecacheUrls` / `computeCacheVersion` / `getCacheName`）。Canvas / DOM / Node API 非依存で単体テスト対象。ビルド時の SW ジェネレーター（`scripts/generate-sw.ts`）と共用
  - `__tests__/` - 単体テスト
- `src/hooks/` - カスタム React フック
  - `usePasteImages.ts` - ページ全体の paste イベントを購読し、クリップボードの画像ファイルをコールバックに渡すフック
  - `useHandoffReceiver.ts` - ハンドオフの受け取り側フック。mount 時に共有ストアからペイロードを consume し、新規アップロードと同じフィルタ（`filterValidFiles` → `addUniqueFilesWithLimit`（`MAX_INPUT_FILES` 上限））を通してページの投入関数へ渡す。戻り値 `{ notice, clearNotice }` で到着バナーの表示情報を返す
- `src/workers/` - Web Worker（変換ページのバッチ処理を並列化）
  - `imageProcessing.worker.ts` - 画像処理 Worker。デコード（createImageBitmap / HEIC / TIFF）→ OffscreenCanvas 描画・リサイズ → エンコード（convertToBlob / `@jsquash/avif`）→ EXIF 挿入 を Worker 内で完結。`mode: "optimize"` のときは冒頭で分岐し、OffscreenCanvas を経由せず `optimizeImageBuffer` で形式維持の再圧縮を行う。`new Worker(new URL(..., import.meta.url), { type: "module" })` で生成し、`next build --webpack` + `output: "export"` でワーカーチャンクが `out/_next/static/` に出力される
  - `imageProcessingPool.ts` - メインスレッド側プール。convert は `isOffscreenPipelineSupported()`、optimize は `Worker` の有無で対応判定し `navigator.hardwareConcurrency` を上限に Worker を起動。Worker 失敗・クラッシュ時は当該ファイルのみメインスレッド（convert は `convertImage` / optimize は `optimizeImage`）にフォールバック。成功時は mode に応じて `buildConversionResult` / `buildOptimizeResult` で結果を組み立て、バッチ完了時に terminate
  - `messages.ts` - Worker とメインスレッド間のメッセージ型
- `src/i18n/` - 国際化設定
- `src/types/` - 外部ライブラリの型定義（piexifjs / exif-js / heic-decode）
- `scripts/` - ビルド補助スクリプト（`tsconfig.json` の `exclude` に含め tsc 対象外）
  - `generate-sw.ts` - postbuild で `out/` を再帰走査し、`precache.ts` の純粋関数でプリキャッシュ URL とバージョンを算出、テンプレートに注入して Service Worker（`out/sw.js`）を生成する。Node 24 の TypeScript 型ストリップ実行（`node scripts/generate-sw.ts`）で動くため tsx 等の追加依存は不要
  - `sw-template.js` - Service Worker 本体テンプレート。`__CACHE_NAME__` / `__PRECACHE_URLS__` を generate-sw.ts が実値へ差し替える
  - `generate-icons.mjs` - PWA アイコン PNG を生成する一度きりのスクリプト（`icon.svg` と Logo.tsx のブランドマークから Playwright の Chromium でラスタライズ。ビルドには組み込まず、生成物をコミットして配信する）
  - `generate-luts.ts` - 編集ページのオリジナルプリセット LUT（`.cube`）を `public/luts/` に出力する一度きりのスクリプト（generate-icons 方式。ビルドには組み込まず、生成物をコミットして配信する。`.ts` のため `scripts/tsconfig.json` の typecheck 対象）
- `public/` - 静的アセット（ビルド時に `out/` へコピーされ配信される）
  - `icons/` - PWA アイコン（`icon.svg` と生成済みの `icon-192.png` / `icon-512.png` / `icon-maskable-512.png`）
  - `luts/` - 編集ページの同梱プリセット LUT（`.cube` 11 本 + ライセンス・出所を明記した `CREDITS.md`）。内訳はアルゴリズム生成のオリジナル（CC0）5 本と freshluts.com 由来の CC0 をクリーン名へリネームした 6 本。動的 fetch で読み込むため初期バンドル非影響だが PWA のプリキャッシュ対象（オフライン利用可）
  - `_headers` - Cloudflare Pages 用ヘッダー設定（`/sw.js` は `no-cache`、`/_next/static/*` は長期 `immutable`、`/manifest.webmanifest` の content-type を明示）

### パスエイリアス
プロジェクトでは `tsconfig.json` で設定された TypeScript パスエイリアスを使用：
- `@/*` → `src/*`
- `@components/*` → `src/components/*`
- `@utils/*` → `src/utils/*`

※ vitest 用のエイリアスは `vitest.config.ts` にも定義されている。エイリアスを追加する場合は両方を更新すること。

### コア機能
1. **画像フォーマット変換 / 最適化** - JPEG、PNG、WebP、AVIF 形式への変換（品質制御付き。AVIF は出力のみ対応）。目標ファイルサイズ (KB) を指定すると品質を二分探索して目標以下で最大品質の結果を出力する（JPEG / WebP のみ。達成不可時は最小サイズで出力し一覧に警告表示）。処理モードのラジオで「フォーマット変換」と「最適化（形式を維持）」を切り替えられ、最適化はフォーマットを変換せず入力と同じ形式のまま再圧縮してサイズを削減する（PNG は可逆・ピクセル不変、JPEG / WebP は高品質で再エンコード。削減できなければ元をそのまま採用。対応は PNG / JPEG / WebP のみで対応外は失敗通知。最適化モード時は変換用の設定（対象フォーマット・品質・目標サイズ・画像サイズ・EXIF 保持）を非表示にする）。HEIC/HEIF（iPhone 写真）と TIFF は変換ページのみ入力として受理（crop / metadata はブラウザがプレビュー描画できないため対象外）。変換・最適化に失敗したファイルは一覧で画面に通知される
2. **画像トリミング** - プレビュー付きのビジュアルトリミングインターフェース。アスペクト比プリセット（自由 / 1:1 / 16:9 / 4:3 / 3:2。自由以外は選択中のドラッグ・8方向リサイズ・移動が比率固定）、右/左 90° 回転・左右/上下反転、適用範囲の切り替え（「全画像一括」／「画像ごと」。画像ごとは ←/→ ナビゲーションと連動して画像単位に領域・回転/反転を保持・適用）に対応。プレビュー時に EXIF Orientation を `createImageBitmap(imageOrientation: "from-image")` で自動補正し回転/反転をキャンバスへ焼き込む（EXIF 保持時は出力の Orientation タグを 1 に正規化して二重回転を防ぐ）
3. **画像編集（ライト/カラー/ディテール/効果の調整 + 自動補正 + トーンカーブ + LUT フィルタ）** - 写真アプリ相当のライト/カラー/ディテール/効果の調整・自動補正（WB スポイト含む）・トーンカーブ・LUT（Look-Up Table）フィルタを非破壊で行い複数画像へまとめて適用できる `/edit` ページ（Phase 1 MVP = Issue #66、Phase 2 LUT = Issue #67、トーンカーブ / 自動補正 / WB スポイト / ディテール系 / モノクロ・ガンマ = Issue #68 の第 2〜6 項目）。ライト（露光量 / ガンマ / 輝度 / コントラスト / ハイライト / シャドウ / 白レベル / 黒レベル）とカラー（彩度 / 自然な彩度（Vibrance）/ 色温度 / 色合い（Tint）/ 色相 / モノクロ）、ディテール（シャープネス / 明瞭度）、効果（ビネット / グレイン）を各項目リセット + 全体リセット付きで調整する（モノクロは 0/100 のチェックボックストグルで、色温度 / 色合い / 色相がモノクロ化前に効くため B&W のカラーフィルタとして使える（彩度スライダーと独立）。シャープネス / グレインは 0 起点の片方向スライダー。グレインは座標ハッシュの決定的ノイズでプレビューと出力・GPU と CPU で同じ粒。ビネットは負値で周辺増光）。調整パネル先頭の「自動補正」（オートレベル / 自動ホワイトバランス）はワンショットボタンで、編集前ヒストグラムの統計から既存スライダー値を逆算してセットする（オートレベルは輝度ヒストグラムのパーセンタイルクリップ（片側 0.5%）で黒点・白点を推定して黒レベル / 白レベルを、自動ホワイトバランスは gray-world で色温度 / 色合いをセット。結果はスライダーに可視化されそのまま手動微調整・個別リセットでき、編集前統計基準のため同じ画像で再押下しても値は不変（冪等）。「すべてリセット」でリセットされる）。「WB スポイト」はトグルでモード化し、プレビュー上の「無彩色にしたい点」をクリックすると編集前ソースの 5×5 近傍平均から色温度 / 色合いを逆算してセットする（同じ点の再クリックで値不変（冪等）。クリック適用後は自動解除のワンショットで、Esc / トグル再クリック / 画像切替 / リストクリアでも解除。モード中は crosshair カーソル + ヒント文がスポイト用に切り替わり分割ドラッグは開始しない。Before/After のどちら側をクリックしても同じソース画素に写像される）。トーンカーブは RGB（マスター・全チャンネル同一適用）/ 輝度の 2 チャンネルを SVG エディタで編集でき（プロット領域のクリックで制御点を追加してそのままドラッグ配置・ドラッグで移動・ダブルクリックで削除。端点は x 固定で y のみ可動・最大 16 点。背景に編集前（カーブ適用前）の輝度ヒストグラムを重ねて表示）、チャンネル単位の「カーブをリセット」に対応する（全体リセットにも含まれる）。LUT フィルタは同梱プリセット（11 本）から選ぶか、`.cube`（1D / 3D）または HALD CLUT PNG をカスタムアップロードして適用でき、適用強度（0-100）で元色とのブレンド比を調整できる（適用順は ディテール → 調整 → トーンカーブ → LUT → ビネット / グレイン に固定）。プレビューは WebGL2 フラグメントシェーダでリアルタイム反映し、非対応環境は Canvas2D CPU パスへ自動フォールバック（プレビューと出力は同一の描画経路 `applyAdjustmentsToCanvas` を通すため結果が一致＝WYSIWYG）。分割スライダーの Before/After 比較、適用範囲トグル（全画像一括 / 画像ごと。crop と同じ dual-store で ←/→ と連動。調整・トーンカーブ・LUT は同じトグルを共有）に対応。右カラム先頭（調整スライダーの上）には調整・トーンカーブ・LUT 適用**後**のプレビュー結果を反映するヒストグラム（RGB / 輝度の表示切替。黒/白レベル調整の指標、Issue #68 の第 1 項目）を常時表示し、スライダー操作にリアルタイム追従する（rAF で 1 フレーム 1 回に間引き。WebGL2 非対応の CPU フォールバック環境でも同一経路で表示）。出力は元形式維持（既定）または形式選択（JPEG/PNG/WebP/AVIF）で、EXIF 保持は AVIF 以外で有効（向き焼き込みに合わせ Orientation を 1 に正規化）。受理形式は crop と同じ UPLOAD_FORMATS（JPEG/PNG/WebP/BMP。HEIC/TIFF はプレビュー描画不可のため対象外）。当面はメインスレッド逐次処理（crop 同様）
4. **EXIF メタデータ管理** - EXIF データの表示（JPEG / PNG / WebP に対応）、編集、選択的削除。GPS 位置は「削除」に加えて「市区町村レベルに丸める」（約 1km 精度で残す）を選択可能（GPS タグがある場合のみ UI 表示。丸めは JPEG のみ有効で、その他の形式は Canvas 再描画により全メタデータを削除）。変換・トリミング・編集の「EXIF を保持」は JPEG / PNG / WebP 出力で有効（AVIF は非対応）
5. **ツール連携（ハンドオフ）** - 変換・最適化 / トリミングの結果をダウンロードせずに次のツールへ引き継いで続けて処理できる（Phase 1 = Issue #70 は convert ⇄ crop。metadata は #71、edit は #72 で受け取り配線予定）。結果画面に「この結果を次のツールへ（ダウンロード不要で続けて処理）」の送り先ツール別ボタンを表示する（候補は「現在のツール以外」かつ「全結果 MIME を受理できるツール」のみ。例: AVIF 結果は受理できるツールがないため導線ごと非表示）。受け取りページには到着バナー（`role="status"`。「変換の結果 N 件を引き継ぎました」+ 取り込めなかった件数の補足 + 閉じるボタン）を表示し、引き継ぎは新規アップロードと同じフィルタ（MIME・重複除外・`MAX_INPUT_FILES`（200 件）上限）を通る。ペイロードは File 実体のみを in-memory 保持（ObjectURL・ストレージ・URL 非経由。リロードで消える）し、送出時に元ページの結果はクリアされ ObjectURL は revoke される。ファイル名は既存命名のまま連鎖する（例: sample.png → sample.jpeg → sample_cropped.jpeg）
6. **バッチ処理** - 複数画像の一括処理（ドラッグ&ドロップ / ファイル選択 / クリップボード貼り付け / フォルダドロップで投入）。一度に取り込める件数はハード上限 `MAX_INPUT_FILES`（200 件）までで、フォルダの再帰取込や複数選択で上限を超えた分は取り込まず非ブロッキングの警告を表示する（大量投入時のフリーズ/メモリ圧迫を防ぐため）。変換ページは対応環境で Web Worker + OffscreenCanvas のプール（`navigator.hardwareConcurrency` を上限に並列）で処理し UI をブロックしない（非対応環境はメインスレッド逐次処理にフォールバック）
7. **プライバシーファースト** - Canvas API / WASM / WebGL を使用したクライアントサイドでの全処理
8. **PWA（オフライン対応・インストール可能）** - Web App Manifest とビルド時生成の Service Worker により、一度アクセスすれば JS/CSS/フォント/WASM/HTML/アイコンがプリキャッシュされ、オフラインでも全機能が動作する。対応ブラウザ（主に Chromium 系）ではホーム画面/デスクトップへインストール可能（`beforeinstallprompt` を受けた控えめな導線を表示）

### 重要なパターン
- 画像処理はクライアントサイド操作のために Canvas API を使用
- 変換ページのバッチ処理は Web Worker + OffscreenCanvas のワーカープール（`src/workers/`）で並列実行する。`navigator.hardwareConcurrency` を上限に Worker を起動し、デコード〜エンコード〜EXIF 挿入まで Worker 内で完結させメインスレッド（UI）をブロックしない。AVIF の WASM エンコード（従来メインスレッドで同期実行し UI をフリーズさせていた）も Worker 内で実行する。OffscreenCanvas / Worker / createImageBitmap 非対応環境（`isOffscreenPipelineSupported()` で判定）ではメインスレッド逐次処理にフォールバックし、Worker が個別に失敗・クラッシュした場合も当該ファイルのみ `convertImage` で再試行する。メインスレッドと Worker が共有する型・純粋ロジックは Canvas 非依存の `conversionCore.ts` / `concurrency.ts` / `pngQuality.ts` に集約し、Worker バンドルへの Canvas コード混入を防ぐ
- AVIF エンコードのみ Canvas の `toBlob("image/avif")` が全ブラウザ未実装のため、`@jsquash/avif`（WASM）を動的 import で使用（AVIF 変換実行時のみロードされ、初期バンドルに影響しない。処理はブラウザ内で完結）
- 最適化（形式を維持）モードはフォーマットを変換せず、入力と同じ形式のまま再圧縮してサイズを削減する（Issue #61）。エンジンは形式ごとに動的 import で使い分ける: PNG は `@jsquash/oxipng`（バイト列→バイト列の真の可逆最適化・ピクセル不変・Canvas 不要）、JPEG は `@jsquash/jpeg`（mozjpeg。progressive + trellis 量子化での高品質再エンコードで実質劣化最小）、WebP は `@jsquash/webp`（ロスレス VP8L 入力はロスレス再エンコード、ロッシー入力は高品質ロッシー再エンコード）。いずれも最適化後が元以上なら元をそのまま採用する（no-worse-than-original）。メタデータの扱いは codec の能力に応じて非対称: JPEG は decode 時に `preserveOrientation: true` で EXIF Orientation をピクセルへ焼き込み（crop 経路と同じ WYSIWYG。タグ無し再エンコードでも正しい向きで表示される）、その他の EXIF は再エンコードにより除去される。WebP は `@jsquash/webp` が decode 時に Orientation を補正できず再エンコードで EXIF も失われるため、EXIF を持つ WebP は最適化せず元を採用して向きの崩れ・メタデータ喪失を防ぐ（EXIF 無しの WebP のみ再圧縮）。PNG は oxipng のバイト列最適化のためメタデータを保持する。対応形式は PNG / JPEG / WebP のみで、対応外（BMP / TIFF / HEIC / AVIF 等）は失敗として一覧通知される。jsquash はバッファを直接処理し OffscreenCanvas が不要なため、バッチは `Worker` があれば（OffscreenCanvas 非対応環境でも）ワーカープールで各形式のまま並列最適化し（混在バッチ可）、Worker 不在・個別失敗時はメインスレッドの `optimizeImage` にフォールバックする。形式→エンジンのディスパッチ・no-worse-than-original 判定・WebP のロスレス/ロッシー判定は Canvas 非依存の純粋関数 `optimizeCore.ts`（単体テスト対象）に集約し、jsquash を動的 import するブラウザ側オーケストレーションは `imageOptimizer.ts`（メイン/Worker 共用の `optimizeImageBuffer`・メインスレッド用 `optimizeImage`）が担う。UI の処理モード切替は `src/app/convert/components/ConversionSettings.tsx`（文言は i18n `convert.mode` / `convert.modeConvert` / `convert.modeOptimize` / `convert.optimizeDescription` など）
- HEIC/HEIF のデコードは libheif の WASM ビルド（`heic-decode` + `libheif-js`）を使用し、動的 import により HEIC 変換時のみロードする（初期バンドルに影響なし）
- TIFF のデコードは `utif2`（純 JS の TIFF デコーダー）を使用し、動的 import により TIFF 変換時のみロードする（初期バンドルに影響なし。マルチページ TIFF は先頭ページのみ対応）
- HEIC / TIFF は MIME タイプが特定されない環境があるため、拡張子（.heic/.heif/.tif/.tiff）によるフォールバック判定を行う（`fileUtils.ts` の `FORMAT_EXTENSION_FALLBACKS`。`isHeicFile` / `isTiffFile` の判定と input の accept 属性の両方で使用）
- 画像の投入は全ページ共通の `FileUploadArea` に集約し、ドラッグ&ドロップ / ファイル選択 / クリップボード貼り付け（Ctrl/Cmd+V、`usePasteImages` フック）/ フォルダドロップ（`directoryReader.ts` による再帰走査）のいずれも共通関数 `addFiles`（`filterValidFiles` による MIME フィルタ → `addUniqueFilesWithLimit` による重複除外 + 上限件数 `MAX_INPUT_FILES`（200 件）での切り詰め）に合流させる。上限超過時は超過分を取り込まず、`role="alert"` の非ブロッキング警告ボックス（i18n `fileUpload.limitExceeded`）を表示する（件数の減少＝削除・クリアでリセット。判定は `fileUtils.ts` の `shouldClearLimitWarningOnDecrease`。「件数 < 上限」だけで消すと、走査打ち切り警告を追加操作直後の再描画で消してしまうため件数の減少を条件にする）。フォルダドロップは drop イベント中に `webkitGetAsEntry()` を同期取得してから再帰走査する（非対応環境は `dataTransfer.files` にフォールバック）。走査は `collectFilesFromEntries(entries, { maxFiles: MAX_INPUT_FILES + 1, accept })` で有界化し（`accept` は各ページの受理形式判定 `isAcceptedFileType`）、`readEntries` を 1 バッチずつストリーミング処理することでフラットな巨大フォルダでも Entry ハンドルが無制限に積み上がらないようにする。走査自体が上限で打ち切られた場合は戻り値の `reachedLimit` を `addFiles(files, collectionTruncated)` に渡し、重複除外後の切り詰め（`truncated`）と OR で取りこぼし警告に連動させる。クリップボード取り込みは `fileUtils.ts` の `getFilesFromClipboardData`（`.files` 優先、無ければ `.items` の `kind === "file"`）
- ツール連携（ハンドオフ）は純粋ロジック `handoff.ts`（単体テスト対象）と React への配線（`HandoffContext.tsx` / `useHandoffReceiver.ts` / `HandoffSend.tsx` / `HandoffNotice.tsx`）に分離する（Issue #70, Phase 1）。ペイロード（File[]）は「1 回のナビゲーションだけ生存」するストア（`createHandoffStore`）で受け渡し、ストア本体はモジュールスコープのシングルトンに置く（Next.js のクライアント遷移ではタイミングによりルートレイアウトごと再マウントされることがあり、Provider の ref 保持では送出直後のペイロードが失われるため）。即時クリアの consume-once にはせず到着ページの間は冪等に consume できるようにし（App Router の二重マウントによる取りこぼし防止）、一方で送出元ページには配送せず、到着後に別パス名から consume / onNavigate された時点で破棄して戻る操作での二重取り込みを防ぐ。`HandoffSend` はマウント時に送り先ルートを `router.prefetch` する（未プリフェッチの `router.push` は RSC 取得失敗時にフル MPA ナビゲーションへフォールバックし、in-memory のペイロードがドキュメントリロードで失われるため）。受け取り側（`useHandoffReceiver`）は新規アップロードと同じフィルタ（`filterValidFiles` → `addUniqueFilesWithLimit`）を通してからページの投入関数へ渡す。ツールのメタ定義（パス・ラベルキー・受理形式・受け取り可否）は `HANDOFF_TOOLS` を Navigation と共有する単一の真実とする。UI 文言は i18n `handoff.*`
- トリミングの座標計算（境界クランプ・表示⇔自然座標変換・アスペクト比の当てはめ/強制・回転後寸法・画像ごとの領域/変換の解決）は Canvas / DOM 非依存の純粋関数 `cropGeometry.ts` に集約し、CropSelector（表示座標での操作。`aspectRatio` prop で比率固定リサイズ）・crop ページ（自然座標での state 保持と適用範囲トグル）・`imageCropper.ts`（出力）で共有して単体テストする。回転/反転と EXIF Orientation は `renderOrientedImage` でプレビューと出力の双方に同一ロジックで焼き込み（WYSIWYG）、EXIF 保持時のみ `normalizeExifForBakedImage` で出力 TIFF の Orientation を 1 に揃え、実ピクセル寸法タグ（PixelXDimension/PixelYDimension）が存在すれば出力寸法へ更新する。トリミングツールバー UI は `src/app/crop/components/CropToolbar.tsx`（文言は i18n `crop.*`）
- 画像編集（ライト/カラー/ディテール/効果の調整 + トーンカーブ + LUT フィルタ）はプレビュー・出力とも同一の `applyAdjustmentsToCanvas`（`webglImageRenderer.ts`）を通す（WYSIWYG）。既定は WebGL2 フラグメントシェーダ（`adjustmentShader.ts`）でリアルタイム描画し、`createAdjustmentRenderer` が null を返す環境（WebGL2 非対応）は Canvas2D + `effects.ts`（`detailDeltaAt` / `vignetteFactorAt` / `grainNoiseAt`）+ `applyAdjustmentToPixel`（`adjustments.ts`）+ `applyToneCurveToPixel`（`toneCurve.ts`）+ `applyLutToPixel`（`lutParser.ts`）の CPU パスへフォールバックする。GPU（GLSL）と CPU（TS）が結果一致する構造的担保として、ピクセル単位の色調整数式は `adjustments.ts` の `applyAdjustmentToPixel` を唯一の真実とし、GLSL 側は同じ順序・同じ係数・同じクランプ位置でミラーする（キー→uniform 名の対応表 `ADJUSTMENT_UNIFORMS` も単一の真実として配線・アップロード・単体テストのガードが共有）。バッチ（`editImages`）は WebGL レンダラを 1 個だけ生成して全ファイルで使い回し、finally で必ず dispose する（コンテキスト枯渇を防ぐ。crop 同様メインスレッド逐次処理で、Worker + OffscreenCanvas 化は将来の最適化）。調整は非破壊で `AdjustmentState`（UI 単位 [-100,100]）として保持し、書き出し時に `normalizeAdjustments` で [-1,1] に正規化してから適用する。適用範囲（全画像一括 / 画像ごと）は crop の dual-store パターンを踏襲する（`resolveAdjustmentForIndex`）。UI は `AdjustmentPanel`（`ConversionSettings` の不変更新契約を踏襲）/ `EditToolbar` / `CompareView` / `ToneCurvePanel` / `LutPicker` / `HistogramPanel`（文言は i18n `edit.*` / `edit.toneCurve.*` / `edit.lut.*` / `edit.histogram.*`。適用範囲・画像ナビは `crop.*` を共用、失敗通知の見出しは `ConversionErrors` の `titleKey` prop で `edit.editFailures` に差し替え）。ヒストグラム（RGB / 輝度）は調整・トーンカーブ・LUT 適用後のプレビューを反映する: `CompareView` が編集後キャンバス（GPU / CPU フォールバックのどちらでも転写済みの 2D キャンバス）を `resolveHistogramSampleSize` で縮小 `drawImage` → `getImageData` した ImageData を optional prop `onEditedFrame` で渡し（連続再描画は rAF で 1 フレーム 1 回に間引き・サンプリング用キャンバスは ref で使い回し・コールバックは ref 経由で参照し GPU 再描画を誘発しない）、page 側が `histogram.ts` の `computeHistogram` で 256 ビンの R/G/B/輝度に集計して `HistogramPanel`（SVG パス描画・RGB/輝度チップ）が表示する（「リストをクリア」でリセット）
- LUT フィルタ（色変換フィルタ）は写真編集の標準順に従い調整（`applyAdjustmentToPixel`）の**後段**に適用する（Issue #67, Phase 2）。GPU（`adjustmentShader.ts` の GLSL）と CPU（`lutParser.ts` の `applyLutToPixel`）はドメイン正規化 → 3D テクスチャのトライリニア lookup → 強度ブレンドを同順序でミラーし WYSIWYG を担保する（`applyAdjustmentToPixel` を唯一の真実とするのと同方針。LUT サンプラー名 `LUT_SAMPLER` と uniform 名 `LUT_UNIFORMS` を配線の単一の真実にして GLSL 生成・アップロード・単体テストのガードが共有）。`.cube`（1D / 3D）と HALD CLUT はパース時に常に正規化済み 3D `LutData` へ揃える（1D は分離可能変換として 3D へ厳密展開）ため下流は単一の 3D グリッドだけを扱う。GPU は LUT を WebGL2 の 3D テクスチャ（内部形式 RGB8・LINEAR サンプリングがトライリニア補間を担う）としてアップロードし、既定は恒等 LUT を入れて LUT 未選択でも常時 complete に保つ。パース・CPU 適用の純粋ロジックは `lutParser.ts`（単体テスト対象）、選択状態と dual-store 解決は `lutState.ts`（単体テスト対象）、fetch / FileReader / Canvas デコードのブラウザ依存の橋渡しは `lutLoader.ts`（E2E 検証）に分離する。`AdjustmentState` は不変のまま、LUT は並列の軽量選択 state（`lutId` + `strength`）として持ち、重いデータ本体は edit ページの ref レジストリ（`lutId → LutData`）で参照する。プリセットの実データは `public/luts/*.cube` を動的 fetch し（初期バンドル非影響・モジュール内キャッシュ）、依存追加はなし（WebGL2 / Canvas はブラウザ標準）
- トーンカーブ（RGB マスター / 輝度チャンネル）は調整（`applyAdjustmentToPixel`）の後・LUT の前に適用する（Issue #68 の第 2 項目。適用順は ディテール → 調整 → トーンカーブ → LUT → ビネット / グレイン に固定）。制御点列（正規化 [0,1]²・端点 x 固定）を Fritsch–Carlson の単調 3 次 Hermite 補間で 256 エントリの 1D LUT へ焼成し（`buildToneCurveTable`。RGBA インターリーブの Float32Array(256×4) で `.rgb`=各チャンネル（現状マスター同値・将来 R/G/B 個別カーブへ拡張可能）/ `.a`=輝度）、GPU は 256×1 RGBA8 テクスチャ（TEXTURE2・既定は恒等）の LINEAR サンプリング + テクセル中心補正、CPU は同式の floor+lerp（`sampleCurveTable`）で lookup して WYSIWYG を担保する（テーブルは焼成時に 8bit 量子化済みのため GPU テクセルと CPU 参照値も同一）（サンプラー名 `CURVE_SAMPLER` と uniform 名 `CURVE_UNIFORMS` を配線の単一の真実にするのは LUT と同方針）。輝度カーブは既存トーン調整と同じ加算シフト方式（`curve(luma) - luma` を全チャンネルへ加算。luma は Rec.709）。恒等カーブはテーブルを焼成せず null を渡してサンプリング自体をスキップする（出力時は同一 `ToneCurveState` のテーブルを Map で重複焼成しない）。点操作・補間・焼成・CPU 適用・dual-store 解決（`resolveToneCurveForIndex`）は純粋ロジック `toneCurve.ts`（単体テスト対象）に集約し、`ToneCurvePanel` は座標変換とイベント処理に徹する。適用範囲（全画像一括 / 画像ごと）は調整・LUT と同じ `applyToAll` トグルを共有する
- 自動補正（オートレベル / 自動ホワイトバランス / WB スポイト）は新しい描画パイプライン段を追加せず、編集前の統計から既存の調整スライダー値を逆算してセットする「統計 → スライダー値」方式（Issue #68 の第 3・4 項目。描画パイプライン（`applyAdjustmentToPixel` / GLSL / `EditJob` / `webglImageRenderer.ts` / `imageEditor.ts`）は一切不変）。オートレベルは編集前ヒストグラム（トーンカーブ背景用に分離済みの `sourceHistogram`）の輝度パーセンタイルクリップ（片側 0.5% = `AUTO_LEVELS_CLIP_RATIO`）で黒点・白点を推定し、パイプラインのトーンマスク重み（`blacksToneWeight` / `whitesToneWeight`）で逆算した blacks / whites をセットする。WB の逆算式は「点の色 → temperature / tint」の `computeWhiteBalanceForNeutralPoint` を単一の真実とし、自動ホワイトバランス（gray-world）はチャンネル平均をこの関数へ渡す特殊形、WB スポイトはクリック点の近傍平均を渡す形で共有する（`TEMPERATURE_SHIFT` / `TINT_SHIFT` 等の逆算係数は `adjustments.ts` が輸出し `applyAdjustmentToPixel` との整合を単一の真実として保つ）。WB スポイトは `AdjustmentPanel` のトグル（`aria-pressed`）でモード化し、プレビューのクリック点を `displayPointToSourcePixel` でソース自然座標へ写像 → **編集前ソース**（`previewSource`。getImageData で読むため型を `HTMLCanvasElement` に絞って保持）の 5×5 近傍（`WB_SAMPLE_RADIUS` / `clampSampleWindow` で読み出しを有界化）を `averageRgb` で平均して逆算する（編集後キャンバスから読むと補正結果が次のサンプル値へ混入するフィードバックになるため必ず編集前ソース基準。同じ点の再クリックで値不変 = 冪等）。クリック適用後は自動解除（ワンショット）で、Esc / トグル再クリック / 画像切替 / リストクリアでも解除。モード中は crosshair カーソル + `CompareView` のヒント文がスポイト用に切り替わり分割ドラッグを開始しない（Before/After の両キャンバスは同寸重畳のためどちら側のクリックも同じソース画素に写像）。結果はスライダーに可視化され手動微調整・個別リセットの出発点になり（Lightroom の Auto と同方針）、編集前統計基準のため同じ画像で再実行しても値は不変（冪等）。書き込みはいずれも `setCurrentAdjustments` 経由のため適用範囲（全画像一括 / 画像ごと）の dual-store 挙動は手動スライダー操作と完全に同一で、「すべてリセット」でリセットされる。純粋ロジックは `autoAdjust.ts`（単体テスト対象）、UI は `AdjustmentPanel` 先頭の「自動補正」グループ（i18n `edit.auto.*`。編集前統計が未算出の間はボタンを無効化）
- ディテール（シャープネス / 明瞭度）・ビネット / グレイン・モノクロ / ガンマ（Issue #68 の第 5・6 項目）は既存の単一シェーダパイプラインの拡張として実装する。パイプラインは「ディテール（ソース直後）→ 調整（露光 → ガンマ → 輝度 → コントラスト → トーン → 温度/色合い → 彩度 → vibrance → 色相 → モノクロ）→ トーンカーブ → LUT → ビネット → グレイン → 最終クランプ」に固定（ディテールは `texelFetch` によるソース近傍タップで近傍参照がソーステクスチャだけで完結するため、マルチパス / 中間 FBO 不要の単一シェーダのまま）。ガンマ / モノクロは per-pixel 色変換のため `applyAdjustmentToPixel`（手順 1b / 9b）へ、近傍参照（ディテール = 輝度ベースの unsharp mask。シャープネスはストライド 1 の小半径、明瞭度は解像度適応の整数ストライド `clarityStride` + 中間調マスク）と画素位置依存（ビネット / グレイン）の効果は `effects.ts` へ置き、GLSL は同じ順序・同じ係数（`SHARPNESS_GAIN` 等の係数定数と `GAUSS3_TAPS` カーネルをビルダーが埋め込み。明瞭度ストライドは `EFFECT_UNIFORMS` の `u_clarityStride` で配線）でミラーする。グレインは `Math.random` を使わず lowbias32 整数ハッシュ（TS は `Math.imul`、GLSL は uint 演算でビット同一・上位 24bit のみ float 化）による決定的ノイズで、プレビューと出力・GPU と CPU で同じ粒になる（E2E は ±2/チャンネルで粒一致を検証。fp32/fp64 の丸め差が理論上残るため LUT の GPU/CPU 厳密一致とは基準を分ける）。`AdjustmentState` への 6 キー追加（gamma / monochrome / sharpness / clarity / vignette / grain）だけで dual-store（一括 / 画像ごと）・全体リセット・`ADJUSTMENT_UNIFORMS` の自動配線・`clampAdjustments` の欠損キー 0 補完（後方互換）が既存機構のまま機能するため、page.tsx / `EditJob` / `imageEditor.ts` / `CompareView` は変更なし。値 0 の項目は GPU（uniform 分岐）/ CPU（フラグガード）とも計算を完全スキップし既存プレビュー性能は不変。UI は `AdjustmentPanel` の「ディテール」「効果」グループ（i18n `edit.detail` / `edit.effects` / `edit.sharpness` / `edit.clarity` / `edit.vignette` / `edit.grain` / `edit.gamma` / `edit.monochrome`）
- EXIF データ処理は保存に `piexifjs`、読み取りに `exif-js` を使用。WebP（RIFF の EXIF チャンク）と PNG（eXIf チャンク）は、取り出した TIFF を合成 JPEG（APP1）に包んで exif-js に読ませる（exif-js は先頭が JPEG SOI でないと読めないため）
- EXIF の書き込みは JPEG（piexifjs）に加え、PNG は eXIf チャンク、WebP は VP8X + EXIF チャンクへ挿入する。バイナリ操作は Canvas / ブラウザ API 非依存の純粋関数 `exifBinary.ts`（単体テスト対象）に切り出し、ブラウザ側の読み取り〜書き込みの橋渡しを `exifTransfer.ts` が担う。AVIF はメタデータ書き込み非対応
- テーマ切り替え（ライト/ダーク）は CSS カスタムプロパティで処理
- 言語設定は localStorage に保存
- ファイルダウンロードはバッチ操作に `JSZip` を使用
- ページ別の SEO メタデータ（title / description / OGP / Twitter card / canonical）は `pageMetadata.ts` の `buildPageMetadata` で組み立て、各ルートの `layout.tsx`（`"use client"` を持たないサーバーコンポーネント）から export する（ページ本体が client component で metadata を直接 export できないための構成）。root の `layout.tsx` が `metadataBase`・title テンプレート（`%s | Client-Side Image Converter`）・共通 description・OGP / Twitter 既定値を集約し、トップページ `/` はこの既定値を使う。メタデータは i18next 非経由の静的 HTML なので主言語は日本語（`lang="ja"` / i18n 既定 `lng: "ja"` に合わせる。ロケール JSON の変更は不要）。専用 OG 画像アセットが無いため Twitter card は `summary`（画像追加時に `summary_large_image` へ切り替える想定）
- PWA は静的エクスポート（`output: "export"` + `next build --webpack`）+ Cloudflare Pages の構成に合わせ、ビルドツール非依存の「手書き SW テンプレート + postbuild ジェネレーター」方式で実現している。`scripts/sw-template.js` を `scripts/generate-sw.ts` がビルド後に処理し `out/sw.js` を出力する（`npm run build` の `postbuild` チェーン: `next-sitemap && node scripts/generate-sw.ts`）。プリキャッシュ判定・URL 変換・バージョン算出は純粋関数 `precache.ts` に切り出して単体テストする
- キャッシュ更新戦略: プリキャッシュ対象は `out/` 全ファイル − denylist（`sw.js` / `_headers` / `robots.txt` / `sitemap*.xml` / `*.map`）。プリキャッシュ URL 一覧のハッシュ（FNV-1a）をキャッシュ名（`wic-precache-<version>`）に含めるため、内容ハッシュ付きの `_next/static/**` が変わったときだけバージョンが変わる。SW は install で `addAll` によるプリキャッシュ、activate で現行バージョン以外の旧キャッシュ削除 + `clients.claim`（`skipWaiting` は付けず更新は次回起動時に反映しセッション中の突然の差し替えを防ぐ）、fetch はハッシュ付きアセット cache-first + ナビゲーションは cache → network → キャッシュ済み `/` フォールバック
- Service Worker の登録は `ServiceWorkerRegister` が `process.env.NODE_ENV === "production"` のときだけ行う（dev で古いキャッシュが配信される事故を防ぐ）。theme-color は root `layout.tsx` の `viewport` export でライト（#f9fafb）/ダーク（#0e1117）を出し分ける。`_headers` で `/sw.js` を `no-cache` にしデプロイごとに新しい SW を確実に取得させる（public/ 配下は `out/` にコピーされて配信される）

## テスト方針

- テストランナーは vitest、DOM 環境は happy-dom（`File` / `FileReader` / `localStorage` などのブラウザ API を使用するため）
- テストファイルはテスト対象と同じディレクトリの `__tests__/` に `<対象ファイル名>.test.ts` として配置する
- `src/utils/` のロジックを追加・変更した場合は、対応する単体テストを追加・更新すること
- EXIF 処理のテストは piexifjs でフィクスチャ（EXIF 入り JPEG）を生成して実データで検証する（`metadataManager.test.ts` 参照）
- Canvas API や WASM / WebGL に依存する処理（描画・変換・エンコード）は happy-dom では動作しないため、単体テストの対象外とする（純粋ロジック部分を切り出してテストする）。編集ページも同様で、色調整の数式（`adjustments.ts` の `applyAdjustmentToPixel`（ガンマ / モノクロ含む）等）・ディテール / ビネット / グレインの数式（`effects.ts` の `detailDeltaAt` / `vignetteFactorAt` / `lowbias32` / `grainNoiseAt` 等。ガウスカーネルの対称性・ハッシュの決定性と乗数のビット同一ガードを含む）・シェーダ生成/配線（`adjustmentShader.ts`）・LUT のパースと CPU 適用（`lutParser.ts` の `parseCubeLut` / `applyLutToPixel` / `haldClutToLutData` 等）・LUT の選択解決（`lutState.ts` の `resolveLutForIndex` 等）・トーンカーブの点操作・補間・テーブル焼成と CPU 適用（`toneCurve.ts` の `addCurvePoint` / `evaluateCurve` / `buildToneCurveTable` / `applyToneCurveToPixel` 等）・ヒストグラムのビン計算と SVG パス生成（`histogram.ts` の `computeHistogram` / `resolveHistogramSampleSize` / `buildHistogramPath` 等）・自動補正 / WB スポイトのスライダー値逆算とサンプリング（`autoAdjust.ts` の `histogramPercentileRange` / `computeAutoLevels` / `computeWhiteBalanceForNeutralPoint` / `computeAutoWhiteBalance` / `clampSampleWindow` / `averageRgb` / `displayPointToSourcePixel` 等。逆算値を `applyAdjustmentToPixel` へ通すクロスチェック・gray-world との等価性回帰を含む）を純粋ロジックとして単体テスト（`adjustments.test.ts` / `effects.test.ts` / `adjustmentShader.test.ts` / `lutParser.test.ts` / `lutState.test.ts` / `toneCurve.test.ts` / `histogram.test.ts` / `autoAdjust.test.ts`）し、WebGL/Canvas を使う実描画（LUT の GPU/CPU 出力ピクセル一致・強度 0 での無効化・プリセット適用・不正ファイル通知、シャープネスのエッジ増強と平坦部不変・ガンマの中間調上昇と白不変・モノクロの WYSIWYG・ビネットの両方向（減光 / 増光）と中心不変・グレインの決定性と GPU/CPU 粒一致（±2/チャンネル許容）と WebGL2 無効化時のフォールバック適用、トーンカーブの点追加によるプレビュー・出力の一致（WYSIWYG）とリセット復帰・輝度チャンネルの色味維持・WebGL2 無効化時の CPU 適用、ヒストグラムの輝度スパイク位置・露光量調整への追従・RGB/輝度切替・WebGL2 無効化時の表示、自動補正のオートレベルによるレンジ拡張とスライダー可視化・冪等性・自動ホワイトバランスのチャンネル平均等化・WebGL2 無効化時の適用、WB スポイトのクリック点基準の補正（gray-world なら逆符号になる 2 色画像での証明）・冪等性・分割位置不変・適用後の自動解除・Esc / 再クリック解除とヒント切替・WebGL2 無効化時の適用を含む）は E2E（`e2e/edit.spec.ts`。`.cube` フィクスチャは `e2e/helpers/fixtures.ts` で実行時生成）で検証する
- Canvas 依存の動作は Playwright E2E（`e2e/`）で実ブラウザ検証する。ダウンロード物はマジックナンバーや piexifjs のバイナリ解析で中身まで検証する（`e2e/metadata.spec.ts` 参照）
- ツール連携（ハンドオフ）はツールメタの整合・送り先解決・File[] 変換・ストアセマンティクスの純粋ロジックを単体テスト（`handoff.test.ts`）し、実際のページ間の受け渡しは E2E（`e2e/handoff.spec.ts`: convert→crop / crop→convert の連鎖処理・AVIF 結果での導線非表示・戻る操作での二重取り込み防止）で検証する。ペイロードが in-memory のため E2E は `page.goto`（フルロード）ではなくアプリ内ボタンによるクライアントサイド遷移で検証する
- E2E のフィクスチャはバイナリを置かず `e2e/helpers/fixtures.ts` で実行時生成する
- E2E は本番同等の静的エクスポート（`npm run build` + `serve out`、ポート 3100）に対して実行される。ローカルで高速に回したい場合は `npm run dev -- --port 3100` を別途起動しておけば `reuseExistingServer` により再利用される（CI では常に build + 静的配信）
- PWA のプリキャッシュ判定・URL 変換・バージョン算出は Canvas 非依存の純粋関数 `precache.ts` に切り出して単体テスト（`precache.test.ts`）。Service Worker 本体の fetch/cache 制御は実ブラウザ動作なので Playwright E2E（`e2e/pwa.spec.ts`）で検証する：manifest link / theme-color / manifest 内容の確認と、SW 登録後に `context.setOffline(true)` で全ルート（/・/convert/・/crop/・/edit/・/metadata/）がキャッシュから描画されること
- PWA の合否は Lighthouse の PWA カテゴリが Lighthouse 12 で廃止されたため、DevTools の Application パネルでの installability 確認と `pwa.spec.ts` のオフライン自動検証で担保する

## Claude Code ハーネス

全体像・設計意図・運用上の注意は `docs/HARNESS.md` を参照。

### CI / デプロイ（`.github/workflows/`）

- **ci.yml**: すべての PR と main への push で 2 ジョブを自動実行
  - `check`: `npm ci` → lint → typecheck → test → build
  - `e2e`: Playwright E2E（Chromium）。失敗時は playwright-report をアーティファクト保存
- **deploy.yml**: main への push で Cloudflare Pages へ本番デプロイ、PR ではプレビューデプロイ + URL を PR にコメント
- **Dependabot（`.github/dependabot.yml`）**: npm と GitHub Actions の依存を毎週月曜 09:00（JST）にチェックし更新 PR を作成（minor / patch は 1 PR にグループ化、open PR 上限 5）
- **main はブランチ保護済み**: `check` と `e2e` の両方が緑でないとマージ不可（管理者含む）
- CI の Node は 24 に固定（npm 10 系では lockfile 検証の挙動差で `npm ci` が失敗するため変更しない）
- `package-lock.json` が壊れた場合（CI の `npm ci` だけが失敗する場合）は `rm -rf node_modules package-lock.json && npm install` でゼロから再生成する
- Cloudflare 認証情報は GitHub Actions シークレットとローカルの `.env`（gitignore 済み）にのみ置く。**リポジトリにコミットしないこと**

### フック・権限（`.claude/settings.json`）

- **PostToolUse (Write|Edit)**: ファイル編集後に Biome で自動フォーマット。編集直後にファイルが書き換わることがあるため、編集が失敗する場合はファイルを読み直すこと
- **PostToolUse (Bash: gh pr create)**: PR 作成を検知し、自動レビューフロー（下記）の開始を指示（スクリプト: `.claude/hooks/pr-created.sh`）
- **Stop**: 応答終了時、TS/TSX ファイルに未コミットの変更があれば lint + typecheck + test を自動実行（スクリプト: `.claude/hooks/check-on-stop.sh`）。失敗するとエラー内容が差し戻されるので修正して再度終了すること
- **permissions**: 危険操作のガード。deny（`sudo` / `git push --force` / `.env` 系・`.dev.vars` 系ファイルの読み書き）、ask（`gh pr merge` / `git reset --hard` / `git clean` / `npm run deploy` / `wrangler pages deploy`）

### サブエージェント（`.claude/agents/`）

- **planner**: 非自明なタスク（3ステップ以上）の実装計画を策定し、Sprint Contract（完了条件）を返す
- **docs-sync**: reviewer の前に起動するドキュメント同期。コード変更に合わせて CLAUDE.md / docs/HARNESS.md / README（日英セット）を更新する。変更がドキュメント記載事項（コマンド・構成・ワークフロー・ユーザー向け機能）に触れる場合のみ
- **reviewer**: タスク完了前の独立コンテキストレビュー。Pass/Fail 判定を返す。Fail があれば修正して再レビュー

### コマンド（`.claude/commands/`）

- **/start-issue <Issue番号>**: GitHub Issue を起点にタスクを開始。Issue 専用 worktree（`.claude/worktrees/issue-{番号}/`）とブランチの作成 → planner → 実装 → 検証 → docs-sync → reviewer → PR 作成（自動レビューフロー起動）まで自走。worktree で作業するため複数 Issue の並列作業が可能
- **/review-pr <PR番号>**: PR のコードレビューを実施し、インラインコメントを投稿
- **/resolve-pr-comments <PR番号>**: PR のレビューコメントを読み取り、修正対応・返信を実施

### PR 自動レビューフロー

`gh pr create` で PR を作成すると、フックが以下のフローを自動起動する：

1. サブエージェントが `/review-pr` の手順で PR をレビューし、インラインコメントを投稿
2. 別のサブエージェントが `/resolve-pr-comments` の手順でコメントに対応（修正・返信）
3. 対応結果のサマリーを報告

※ PR 作成コマンドは単独で実行すること（`git push && gh pr create` のような複合コマンドではフックが発火しない）

### ルール（`.claude/rules/`）

- **workflow-orchestration**: Issue 起点のタスク開始（/start-issue）、planner / docs-sync / reviewer / サブエージェントの使い分けと完了前検証の指針
- **self-review**: タスク完了前に docs-sync によるドキュメント同期と reviewer エージェントによるレビューを必須とするルール

## コードスタイルガイドライン
- コードは TypeScript で記述する
- CSS は CSS Modules を使用する（インラインスタイルは使用しない）
- コンポーネントは React の関数コンポーネントを使用する
- コンポーネントの命名は PascalCase を使用する
- 関数の引数・戻り値には型を明記する
- ファイル名はコンポーネント名と同じにする
- コンポーネントのスタイルは、コンポーネントごとに CSS Modules を使用して分割する
- i18next を使用して多言語対応を行う
- コードのフォーマット&Lintチェックには Biome を使用する（2スペースインデント、ダブルクォート）
- コードの可読性を重視し、コメントは必要に応じて記述し、記述する場合は日本語で記述する
- 実装前に既存のコンポーネントの再利用を検討する

## 最近の更新
- ツール連携（ハンドオフ）Phase 1 を追加（Issue #70）。convert（変換/最適化）と crop（トリミング）の処理結果をダウンロードせずに相互へ引き継いで続けて処理できる（metadata は #71、edit は #72 で受け取り配線予定）。結果画面に「この結果を次のツールへ（ダウンロード不要で続けて処理）」の送り先ツール別ボタンを表示し（候補は「現在のツール以外」かつ「全結果 MIME を受理できるツール」のみ。AVIF 結果のように受理できるツールがなければ導線ごと非表示）、受け取りページには到着バナー（`role="status"`・取り込めなかった件数の補足・閉じるボタン）を表示する。引き継ぎは新規アップロードと同じフィルタ（MIME・重複除外・`MAX_INPUT_FILES`=200 上限）を通り、ペイロードは File 実体のみを in-memory 保持（ObjectURL・ストレージ・URL 非経由。リロードで消える）。送出時に元ページの結果はクリアされ ObjectURL は revoke、ファイル名は既存命名のまま連鎖する（例: sample.png → sample.jpeg → sample_cropped.jpeg）。純粋ロジック `handoff.ts`（型 `ToolId` / `HandoffTool` / `HandoffPayload` / `HandoffStore`、メタ定義 `HANDOFF_TOOLS`（Navigation と共有する単一の真実）、送り先候補算出 `resolveHandoffTargets`、結果→File[] 変換 `conversionResultsToFiles` / `cropResultsToFiles`（失敗スキップ・`croppedBlob.type` 使用・ZIP と同じ `_2` 連番の同名一意化）、「1 回のナビゲーションだけ生存」する `createHandoffStore`（到着ページの間は冪等＝二重マウント耐性・送出元へは配送しない・別パス名で破棄＝二重取り込み防止））、`HandoffContext.tsx`（ストア本体はモジュールスコープのシングルトン。ルートレイアウト再マウントで失われないため）、受け取りフック `useHandoffReceiver.ts`（consume → filterValidFiles → addUniqueFilesWithLimit → ページの投入関数）、UI `HandoffSend.tsx`（候補 0 件なら非描画・マウント時に送り先ルートを `router.prefetch`。未プリフェッチの push が MPA フォールバックすると in-memory ペイロードが失われるため）/ `HandoffNotice.tsx` を新設。`Results.tsx` に optional prop `handoffOrigin`、`Navigation` を `HANDOFF_TOOLS` ベースへリファクタ、convert / crop ページに受け取り配線、root layout に `HandoffProvider`、i18n `handoff.*`（日英）を追加。依存追加なし。単体テストは `handoff.test.ts`（24 件）、実ブラウザ検証は `e2e/handoff.spec.ts`（4 件。convert→crop / crop→convert の連鎖 + ZIP 全件検証・AVIF 結果での導線非表示・戻る操作での二重取り込み防止。in-memory のため page.goto ではなくクライアントサイド遷移で検証）。convert / crop / metadata / edit の既存仕様（受理形式・上限・EXIF 保持・出力）は不変
- 画像編集ページ `/edit` にディテール系（シャープネス / 明瞭度 / ビネット / グレイン）とモノクロ変換 / ガンマを追加（Issue #68 の第 5・6 項目を 1 PR で同時実装。編集プリセット・`.cube` 書き出し等の他項目は未実装）。GPU（WebGL2 単一フラグメントシェーダ）/ CPU（Canvas2D フォールバック）共有のパイプラインを「ディテール（ソース直後）→ 調整（露光 → ガンマ → 輝度 → コントラスト → トーン → 温度/色合い → 彩度 → vibrance → 色相 → モノクロ）→ トーンカーブ → LUT → ビネット → グレイン → 最終クランプ」へ拡張（ディテールは `texelFetch` のソース近傍タップで単一シェーダのまま。マルチパス / FBO 不要）。`AdjustmentState` に 6 キー（`gamma`（ライト）/ `monochrome`（カラー、0/100 チェックボックストグル）/ `sharpness`・`clarity`（新グループ「ディテール」）/ `vignette`・`grain`（新グループ「効果」））を追加するだけで dual-store・全体リセット・`ADJUSTMENT_UNIFORMS` の自動配線・`clampAdjustments` の欠損キー 0 補完が既存機構のまま機能するため page.tsx / `EditJob` / `imageEditor.ts` / `CompareView` は変更なし。シャープネス / 明瞭度は輝度ベースの unsharp mask（3×3 ガウス (1,2,1)⊗(1,2,1)/16。シャープネスはストライド 1、明瞭度は解像度適応の整数ストライド `clarityStride`（短辺 200px ごと・下限 2）+ 中間調マスク。GPU は texelFetch・CPU は輝度平面 Float32Array を前処理 1 回構築）、ビネットは画素中心の対角正規化距離 + smoothstep(0.3, 1.0) の周辺減光（負値で増光・アスペクト追従の楕円）、グレインは lowbias32 整数ハッシュ（TS の `Math.imul` と GLSL の uint 演算でビット同一・上位 24bit のみ float 化）による決定的ノイズ（`Math.random` 不使用。GPU/CPU の粒一致を E2E で ±2/チャンネル検証）、ガンマは露光直後（c ≥ 0 が保証される唯一の位置）の冪変換 γ = 2^(-n)、モノクロは色相の後（調整の最後）の luma 化トグル（温度 / 色合い / 色相がモノクロ化前に効く B&W カラーフィルタ。彩度スライダーと独立）。値 0 の項目は GPU（uniform 分岐）/ CPU（フラグガード）とも計算を完全スキップ（既存プレビュー性能不変）。sharpness / grain は 0 起点の片方向スライダー（`ADJUSTMENT_UI_MIN`）。純粋ロジック `effects.ts`（係数定数・`GAUSS3_TAPS`（カーネルの単一の真実。GLSL のタップ行はビルダーがここから生成）・`clarityStride` / `computeLumaPlane` / `blurLumaAt` / `midtoneWeight` / `detailDeltaAt` / `vignetteFactorAt` / `lowbias32` / `hashPixel` / `grainNoiseAt`）を新設、`adjustments.ts` に `DETAIL_ADJUSTMENT_KEYS` / `EFFECT_ADJUSTMENT_KEYS` / `ADJUSTMENT_UI_MIN` と手順 1b（ガンマ）/ 9b（モノクロ）+ `clamp01` / `smoothstep` の export 化、`adjustmentShader.ts` に `EFFECT_UNIFORMS`（`u_clarityStride`）と各 GLSL ブロック（gl_FragCoord → 画像座標の Y-flip 変換込み）、`webglImageRenderer.ts` に `u_clarityStride` アップロードと CPU フォールバックの拡張、`AdjustmentPanel` に「ディテール」「効果」グループ、i18n `edit.detail` / `edit.effects` / `edit.gamma` / `edit.monochrome` / `edit.sharpness` / `edit.clarity` / `edit.vignette` / `edit.grain`（日英）を追加。依存追加なし・全処理クライアントサイド完結。単体テストは `effects.test.ts` を新設 + `adjustments.test.ts` / `adjustmentShader.test.ts` に追加、実ブラウザ検証は `e2e/edit.spec.ts` に 6 件追加（シャープネスのエッジ増強・ガンマの中間調上昇と白不変・モノクロ WYSIWYG・ビネット両方向・グレイン分散 + GPU/CPU 粒一致 ±2・WebGL2 無効化フォールバック）。convert / crop / metadata と編集の既存仕様（ヒストグラム・トーンカーブ・LUT・自動補正・WB スポイト・出力形式・EXIF 保持）は不変
- 画像編集ページ `/edit` に WB スポイト（中性点クリックで色被り補正）を追加（Issue #68 の第 4 項目。ディテール系等の他項目は未実装）。自動補正（PR #76）と同じ「統計 → 既存スライダー値」方式の延長で、`AdjustmentPanel` の自動補正グループ 3 つ目のトグルボタン（`aria-pressed`）でスポイトモードを有効化し、プレビュー上の「無彩色にしたい点」をクリックすると**編集前ソース**の 5×5 近傍平均 RGB から色温度 / 色合いのスライダー値を逆算してセットする。描画パイプライン（`applyAdjustmentToPixel` / GLSL / `EditJob` / `webglImageRenderer.ts` / `imageEditor.ts`）は一切不変。WB 逆算式の単一の真実を `computeWhiteBalanceForNeutralPoint`（点の色 → temperature / tint）へ集約し、gray-world の `computeAutoWhiteBalance` は「チャンネル平均へのスポイト」として同関数を呼ぶ形へリファクタ（挙動不変・等価性の回帰テスト付き）。サンプリングは編集前ソース基準（編集後から読むとフィードバック）で同じ点の再クリックで値不変（冪等）、書き込みは `setCurrentAdjustments` 経由で dual-store（一括 / 画像ごと）は手動操作と同一。モード中は crosshair カーソル + `CompareView` のヒント文がスポイト用に切替・分割ドラッグは開始しない・Before/After のどちら側のクリックも同じソース画素に写像（両キャンバスは同寸重畳）。クリック適用後は自動解除（ワンショット）で Esc / トグル再クリック / 画像切替 / リストクリアでも解除、統計未算出時（画像切替中）は autoDisabled で無効化。全処理クライアントサイド完結・依存追加なし。`autoAdjust.ts` に `computeWhiteBalanceForNeutralPoint` / `WB_SAMPLE_RADIUS`（2 = 5×5）/ `clampSampleWindow`（getImageData の読み出しを最大 5×5 に有界化）/ `averageRgb`（alpha=0 除外は `computeHistogram` と同基準）/ `displayPointToSourcePixel`（表示座標→ソース自然座標）を追加、`CompareView` に optional props `eyedropperActive` / `onEyedropperPick`、`AdjustmentPanel` に props `onToggleEyedropper` / `eyedropperActive`、page に `wbEyedropperActive` state と `handleToggleEyedropper` / `handleEyedropperPick` / Esc keydown（`previewSource` の型は getImageData のため `HTMLCanvasElement | null` に絞る）、i18n `edit.auto.eyedropper` / `edit.auto.eyedropperHint`（日英）を追加。単体テストは `autoAdjust.test.ts` に 17 件追加（逆算・クロスチェック・gray-world との等価性回帰・窓クランプ・平均・座標変換）、実ブラウザ検証は `e2e/edit.spec.ts` に 3 件追加（クリック点基準の証明（gray-world なら逆符号になる 2 色画像で temperature +55）+ 冪等 + 分割位置不変 + 自動解除 / Esc・再クリック解除とヒント切替 / WebGL2 無効化 CPU フォールバック）。convert / crop / metadata と編集の既存仕様（調整・トーンカーブ・LUT・ヒストグラム・出力形式・EXIF 保持）は不変
- 画像編集ページ `/edit` に自動補正（オートレベル / 自動ホワイトバランス）を追加（Issue #68 の第 3 項目。ホワイトバランス スポイト等の他項目は未実装）。調整パネル先頭の「自動補正」グループ（チップボタン 2 個）を押すと、編集前ヒストグラム（トーンカーブ背景用に分離済みの `sourceHistogram`）の統計から**既存の調整スライダー値を逆算してセットする**ワンショットボタン方式で、新しい描画パイプライン段は追加しない（`applyAdjustmentToPixel` / GLSL / `EditJob` / `webglImageRenderer.ts` / `imageEditor.ts` は一切不変）。オートレベルは輝度ヒストグラムのパーセンタイルクリップ（片側 0.5% = `AUTO_LEVELS_CLIP_RATIO`）で黒点・白点を推定しトーンマスク重みで逆算した blacks / whites を（黒点画素 ≈ 0・白点画素 ≈ 1 へストレッチ）、自動ホワイトバランスは gray-world（チャンネル平均の等化）で逆算した temperature / tint をセット。結果はスライダーに可視化され手動微調整・個別リセットの出発点になり（Lightroom の Auto と同方針）、編集前統計基準のため再押下しても値は不変（冪等）。書き込みは `setCurrentAdjustments` 経由のため一括 / 画像ごとの dual-store 挙動は手動操作と同一で「すべてリセット」でリセットされる。全処理クライアントサイド完結・依存追加なし。純粋ロジック `autoAdjust.ts`（`AUTO_LEVELS_CLIP_RATIO` / `histogramPercentileRange` / `computeAutoLevels` / `channelMeansFromHistogram` / `computeAutoWhiteBalance`）を新設し、`adjustments.ts` はパイプライン係数（`blacksToneWeight` / `whitesToneWeight` / `TEMPERATURE_SHIFT` / `TINT_SHIFT`）を逆算用に輸出（インラインリテラルの参照置換のみで数式・挙動は不変。GLSL 側はリテラルのままミラー）。UI は `AdjustmentPanel` に自動補正グループ（props `onAutoLevels` / `onAutoWhiteBalance` / `autoDisabled`）、page に `handleAutoLevels` / `handleAutoWhiteBalance`、i18n `edit.auto.*`（日英）を追加。単体テストは `autoAdjust.test.ts`（既知分布のパーセンタイル・フルレンジ/低コントラスト/単一値/クランプ飽和・gray-world の方向性・逆算値を `applyAdjustmentToPixel` へ通すクロスチェック。`adjustments.test.ts` に係数ガードを追加）、実ブラウザ検証は `e2e/edit.spec.ts`（オートレベルのレンジ拡張 + スライダー可視化 + 冪等性 / 自動 WB の平均等化 / WebGL2 無効化 CPU フォールバック）。convert / crop / metadata と編集の既存仕様（調整・トーンカーブ・LUT・ヒストグラム・出力形式・EXIF 保持）は不変
- 画像編集ページ `/edit` にトーンカーブ（RGB / 輝度チャンネル）を追加（Issue #68 の第 2 項目）。右カラムの調整スライダーと LUT ピッカーの間（パイプライン順）に SVG カーブエディタを配置し、プロット領域のクリックで制御点を追加してそのままドラッグ配置・ドラッグで移動・ダブルクリックで削除できる（端点は x 固定で y のみ可動・削除不可、最大 16 点・隣接最小間隔 0.01。背景に編集前（カーブ適用前）の輝度ヒストグラム（PR #74 の `buildHistogramPath` を再利用。編集後の分布を流用するとカーブ操作で背景が動くため分離）を重畳、チャンネル単位の「カーブをリセット」付き。全体リセット・適用範囲トグル・`hasAdjustments` 判定にも統合）。適用順は 調整 → トーンカーブ → LUT に固定し、プレビューと出力は同一経路（WYSIWYG）・WebGL2 非対応環境は CPU フォールバック。全処理クライアントサイド完結・依存追加なし。純粋ロジック `toneCurve.ts`（型 `CurvePoint` / `CurveChannel` / `ToneCurveState` / `ToneCurveEditState`、点操作 `addCurvePoint` / `moveCurvePoint` / `removeCurvePoint`（拒否時は同一配列参照を返す契約）、Fritsch–Carlson 単調 3 次 Hermite 補間 `evaluateCurve` / `buildCurveLut`、GPU/CPU 共通テーブル焼成 `buildToneCurveTable`（RGBA インターリーブ Float32Array(256×4)。`.rgb`=各チャンネル（現状マスター同値・将来 R/G/B 個別へ拡張可能）/ `.a`=輝度。焼成時 8bit 量子化で GPU テクセルと CPU 参照値を一致）、GPU の LINEAR サンプリングと同式の `sampleCurveTable`（floor+lerp）、CPU 適用 `applyToneCurveToPixel`（RGB マスター → 輝度カーブの加算シフト。Rec.709 luma）、`isDefaultToneCurve` / dual-store 解決 `resolveToneCurveForIndex`）と UI `edit/components/ToneCurvePanel.tsx`（数式は toneCurve.ts へ委譲し座標変換とイベント処理に徹する）を新設。`adjustmentShader.ts` に `CURVE_SAMPLER` / `CURVE_UNIFORMS`（配線の単一の真実）とカーブブロック（調整クランプ後・LUT 前。テクセル中心補正 (v*255+0.5)/256 で CPU と一致）、`webglImageRenderer.ts` に 256×1 RGBA8 テクスチャ（TEXTURE2・既定恒等・参照比較 `lastCurveTable` で再アップロード省略）と `render` / `applyAdjustmentsToCanvas` の末尾オプション引数 `curve`、`imageEditor.ts` の `EditJob` に `curve` を追加。dual-store（一括 / 画像ごと）は調整・LUT と同じ `applyToAll` トグルを共有し、恒等時は null（サンプリングスキップ）・出力時は同一 `ToneCurveState` のテーブルを Map で重複焼成しない。i18n `edit.toneCurve.*`（日英）を追加。単体テストは `toneCurve.test.ts`（25 件。`adjustmentShader.test.ts` に配線ガード + 適用順（カーブが LUT より前）の構造ガードを追加）、実ブラウザ検証は `e2e/edit.spec.ts`（点追加でプレビュー・出力が明るくなる WYSIWYG とリセット復帰・輝度チャンネルの色味（R>G>B の序列）維持・WebGL2 無効化の CPU フォールバック）。convert / crop / metadata と編集の既存仕様（調整・LUT・ヒストグラム・出力形式・EXIF 保持）は不変
- 画像編集ページ `/edit` にヒストグラム表示（RGB / 輝度）を追加（Issue #68 の応用機能第 1 項目）。右カラム先頭（調整スライダーの上）に、調整・LUT 適用**後**のプレビュー結果を反映するヒストグラムを常時表示する（黒/白レベル調整の指標）。RGB（3 チャンネル半透明重畳・共通スケール）/ 輝度の表示切替チップ（既定 RGB、`aria-pressed`）付きで、スライダー操作に rAF で 1 フレーム 1 回に間引いてリアルタイム追従し、WebGL2 非対応環境（Canvas2D CPU フォールバック）でも転写済み 2D キャンバスから読む同一経路で表示される。全処理クライアントサイド完結・依存追加なし。純粋ロジック `histogram.ts`（型 `HistogramData`、`HISTOGRAM_BINS`（256）/ `HISTOGRAM_MAX_SAMPLE_PIXELS`（65536 = 256×256 相当）、`computeHistogram`（輝度は `adjustments.ts` の `LUMA_WEIGHTS`（Rec.709）を再利用・alpha=0 スキップ）/ `resolveHistogramSampleSize` / `histogramMaxCount` / `buildHistogramPath`）と UI `edit/components/HistogramPanel.tsx`（SVG パス描画・RGB/輝度チップ）を新設。`CompareView` に optional prop `onEditedFrame`（編集後プレビューの描画完了ごとに縮小サンプリングした ImageData を渡す。rAF coalesce・サンプリング用キャンバスは ref で使い回し・コールバックは ref 経由で参照し GPU 再描画を誘発しない）、edit ページに `computeHistogram` によるヒストグラムの state 化（「リストをクリア」でリセット）、i18n `edit.histogram.*`（日英）を追加。単体テストは `histogram.test.ts`、実ブラウザ検証は `e2e/edit.spec.ts`（均一グレーの輝度スパイク位置検証（SVG パス `d` のパース）・露光量 +100 でスパイクが右端へ移動・RGB/輝度チップ切替・WebGL2 無効化フォールバック）。convert / crop / metadata と編集の既存仕様（調整・LUT・出力形式・EXIF 保持）は不変
- 画像編集ページ `/edit` に LUT（Look-Up Table）フィルタを追加（Phase 2、Issue #67）。既存のライト/カラー調整の WebGL パイプラインの後段に色変換フィルタを差し込み、同梱プリセット（11 本）から選ぶか `.cube`（1D / 3D）/ HALD CLUT PNG をカスタムアップロードして適用でき、適用強度（0-100）で元色とのブレンド比を調整できる。全処理クライアントサイド完結。GPU（`adjustmentShader.ts` の GLSL・WebGL2 の 3D テクスチャ）と CPU フォールバック（`lutParser.ts` の `applyLutToPixel`）はドメイン正規化 → トライリニア → 強度ブレンドを同順序でミラーし WYSIWYG を担保（LUT サンプラー名 `LUT_SAMPLER` と uniform 名 `LUT_UNIFORMS` を配線の単一の真実に）。純粋ロジック `lutParser.ts`（`parseCubeLut` / `haldClutToLutData` / `createIdentityLut` / `applyLutToPixel`、常に正規化済み 3D `LutData` へ変換）・`lutState.ts`（軽量選択 `LutSelection`・プリセットカタログ `LUT_PRESETS`・dual-store 解決 `resolveLutForIndex`）、ブラウザ側読み込み `lutLoader.ts`（プリセット動的 fetch・`.cube` / HALD PNG デコード）、UI `edit/components/LutPicker.tsx`、生成スクリプト `scripts/generate-luts.ts`、同梱プリセット `public/luts/*.cube`（11 本）+ `CREDITS.md`（内訳: アルゴリズム生成のオリジナル CC0 5 本 + freshluts.com 由来 CC0 をクリーン名へリネームした 6 本）を新設。`adjustmentShader.ts` に `sampler3D` + LUT uniform、`webglImageRenderer.ts` に LUT 3D テクスチャ（TEXTURE1・RGB8・既定は恒等 LUT）と型 `LutApplication`、`imageEditor.ts` の `EditJob` に `lut` を追加。`AdjustmentState` は不変で LUT は並列の軽量 state（実データは edit ページの ref レジストリ）。適用範囲（全画像一括 / 画像ごと）は調整と同じ `applyToAll` トグルを共有。i18n `edit.lut.*`（プリセット名 11 種含む）を日英追加。依存追加なし（WebGL2 / Canvas はブラウザ標準、AVIF 出力は既存 `@jsquash/avif` を再利用）。単体テストは `lutParser.test.ts` / `lutState.test.ts`（`adjustmentShader.test.ts` に LUT 配線ガードを追加）、実ブラウザ検証は `e2e/edit.spec.ts`（既知 LUT の GPU/CPU 出力ピクセル一致・強度 0 無効化・プリセット適用・不正ファイル通知。`e2e/helpers/fixtures.ts` に `.cube` 生成ヘルパー）。convert / crop / metadata と編集の既存仕様（調整・出力形式・EXIF 保持）は不変
- 画像編集ページ `/edit` を新設（Phase 1 MVP、Issue #66）。写真アプリ相当のライト/カラー調整（露光量 / 輝度 / コントラスト / ハイライト / シャドウ / 白レベル / 黒レベル、彩度 / 自然な彩度 / 色温度 / 色合い / 色相）を非破壊で行い複数画像へまとめて適用できる 4 本目の柱。プレビューは WebGL2 フラグメントシェーダでリアルタイム反映し、非対応環境は Canvas2D CPU パスへ自動フォールバック（プレビューと出力は同一の `applyAdjustmentsToCanvas` を通すため WYSIWYG）。分割スライダーの Before/After 比較、適用範囲トグル（全画像一括 / 画像ごと。crop と同じ dual-store）、出力の元形式維持/形式選択（JPEG/PNG/WebP/AVIF）、EXIF 保持（AVIF 以外・Orientation 正規化）に対応。受理形式は crop と同じ UPLOAD_FORMATS（JPEG/PNG/WebP/BMP。HEIC/TIFF は対象外）、当面はメインスレッド逐次処理。純粋ロジック `adjustments.ts`（色調整数式の唯一の真実 `applyAdjustmentToPixel` ほか）/ `adjustmentShader.ts`（GLSL を同順序・同係数でミラー、`ADJUSTMENT_UNIFORMS` 配線の単一の真実）、描画オーケストレーション `webglImageRenderer.ts` / `imageEditor.ts`、汎用 `Slider.tsx`、`edit/` 配下（`page.tsx` / `layout.tsx` / `AdjustmentPanel` / `EditToolbar` / `CompareView`）を新設。`conversionResult.ts` に `buildEditResult`（`_edited` サフィックス）、`Navigation` に「編集」、`ConversionErrors` に `titleKey` prop（変換・編集で共用）、i18n `navigation.edit` / `edit.*` を追加。依存追加なし（WebGL2 はブラウザ標準、AVIF 出力は既存 `@jsquash/avif` を再利用）。単体テストは `adjustments.test.ts` / `adjustmentShader.test.ts` / `conversionResult.test.ts`、実ブラウザ検証は `e2e/edit.spec.ts`（seo-metadata / pwa の対象ルートにも `/edit/` を追加）。convert / crop / metadata の既存仕様は不変。Phase 2（LUT）/ Phase 3（ヒストグラム等）は後続 Issue で対応予定
- 変換ページに「最適化（形式を維持）」モードを追加（Issue #61）。フォーマットを変換せず、入力と同じ形式のまま再圧縮してファイルサイズを削減する。処理モードのラジオ（「フォーマット変換」/「最適化（形式を維持）」）で切り替え、最適化時は変換用設定（対象フォーマット・品質・目標サイズ・画像サイズ・EXIF 保持）を非表示にする。エンジンは形式ごとに動的 import: PNG は `@jsquash/oxipng`（バイト列→バイト列の可逆最適化・ピクセル不変・Canvas 不要）、JPEG は `@jsquash/jpeg`（mozjpeg。progressive + trellis での高品質再エンコード）、WebP は `@jsquash/webp`（ロスレス入力はロスレス、ロッシー入力は高品質ロッシー再エンコード）。最適化後が元以上なら元を採用（no-worse-than-original）。対応は PNG / JPEG / WebP のみで対応外は失敗通知。バッチは既存ワーカープールに `mode: "optimize"` 分岐を追加し、`Worker` があれば混在バッチを各形式のまま並列最適化、非対応環境・Worker 個別失敗時はメインスレッドの `optimizeImage` にフォールバック。Canvas 非依存の純粋ロジック `optimizeCore.ts`（`resolveOptimizeEngine` / `isOptimizableType` / `pickSmallerSize` / `detectWebpEncoding`、型 `OptimizeEngine` / `WebpEncoding`）と jsquash オーケストレーションの `imageOptimizer.ts`（`optimizeImageBuffer` / `optimizeImage`）を新設、`conversionCore.ts` に `ConversionMode` / `ConversionOptions.mode`、`conversionResult.ts` に `buildOptimizeResult` を追加。UI は `ConversionSettings.tsx`（i18n `convert.mode` 系）。依存追加は `@jsquash/oxipng` / `@jsquash/jpeg` / `@jsquash/webp`（いずれも最適化実行時のみ動的 import で初期バンドル非影響）。単体テストは `optimizeCore.test.ts` / `imageOptimizer.test.ts` / `conversionResult.test.ts`、実ブラウザ検証は `e2e/optimize.spec.ts`（PNG ピクセル不変+削減 / JPEG・WebP 削減 / 混在バッチ / UI）。crop / metadata と convert の既存変換仕様（対応形式・EXIF 保持・目標サイズ・AVIF 出力）は不変
- フォルダ再帰取込のメモリ有界化と取りこぼし警告の厳密化（Issue #60、tech-debt）。ユーザー向け挙動（200 件上限・非ブロッキング警告）は不変で内部リファクタ。(1) `directoryReader.ts` の `collectFilesFromEntries` を `readEntries` の 1 バッチごとに処理するストリーミング列挙へ変更（旧: 単一ディレクトリの全エントリを一括バッファする `readAllDirectoryEntries` → 廃止し `readEntryBatch` へ）。`maxFiles` 到達で以降のバッチを読まないため、1 フォルダに数万件フラットでも Entry ハンドルの蓄積が「バッチサイズ × 再帰の深さ」に有界化される。(2) シグネチャを `collectFilesFromEntries(entries, options?: { maxFiles?; accept? }): Promise<{ files; reachedLimit }>` へ変更（旧: `(entries, maxFiles?): Promise<File[]>`）。`accept` を通過したファイルだけをバジェットに数え・収集するため、サイドカー（.xmp/.json 等）や空サブフォルダで先に上限到達して深部の画像を取りこぼす生ファイル数基準の弱点を解消。(3) `FileUploadArea` の `handleDrop` は `collectFilesFromEntries(entries, { maxFiles: MAX_INPUT_FILES + 1, accept: (f) => isAcceptedFileType(f, acceptedTypes) })` を呼び、走査打ち切りの事実 `reachedLimit` を `addFiles(files, collectionTruncated)` へ渡す。警告条件を `truncated || collectionTruncated` に連動させ「重複が多く最終件数は上限以下だが収集は打ち切った」ケースも取りこぼしとして警告する。(4) 上限警告のリセット条件を「件数 < 上限」から「件数の減少（削除・クリア）」へ厳密化（`fileUtils.ts` に純粋関数 `shouldClearLimitWarningOnDecrease`）。追加操作で collectionTruncated 警告を直後の再描画で消す不具合を防ぐ。単体テストは `directoryReader.test.ts` / `fileUtils.test.ts` に追加
- トリミングページを強化（Issue #31）。(1) アスペクト比プリセット（自由 / 1:1 / 16:9 / 4:3 / 3:2。自由以外はドラッグ・8方向リサイズ・移動が比率固定）、(2) 右/左 90° 回転・左右/上下反転 + EXIF Orientation 自動補正（プレビュー時に `createImageBitmap(imageOrientation: "from-image")` で向きを焼き込み、EXIF 保持時は出力 Orientation を 1 に正規化して二重回転を防止）、(3) 適用範囲の切り替え（「全画像一括」／「画像ごと」。画像ごとは ←/→ と連動して画像単位に領域・回転/反転を保持）を追加。座標計算を Canvas 非依存の純粋ロジック `cropGeometry.ts`（型 `CropArea` / `CropTransform` / `CropState`、`ASPECT_RATIO_PRESETS`、`clampCropArea` / `scaleCropArea` / `toDisplayArea` / `fitAspectRatio` / `enforceAspectRatio` / `orientedSize` / `rotateRight` / `rotateLeft` / `resolveCropForIndex`）に切り出し、`imageCropper.ts` に `renderOrientedImage` / `createOrientedPreviewUrl` を新設、`cropImage` に `transform` / `quality` 引数・`cropImages` に `CropJob[]` を追加、`exifTransfer.ts` に焼き込み済み出力へ EXIF を整合させる `normalizeExifForBakedImage`（Orientation を 1 に正規化 + 実ピクセル寸法タグを出力寸法へ更新）を追加。UI ツールバーは `src/app/crop/components/CropToolbar.tsx`（i18n `crop.*`）。単体テストは `cropGeometry.test.ts` / `exifTransfer.test.ts`、実ブラウザ検証は `e2e/crop.spec.ts`（正方形出力 / 90° 回転 / 画像ごと変換）。crop / metadata の受理形式・convert の仕様は不変（TIFF/HEIC は引き続き crop の入力対象外、AVIF は EXIF 保持・Orientation 正規化の対象外）
- フォルダドロップ・複数投入時の件数上限を追加（Issue #55）。フォルダの再帰取込や multiple 選択で大量ファイルを誤投入すると、サムネイル一斉生成（各画像を `readAsDataURL` でメモリ展開し `Promise.all` で一斉デコード）でタブがフリーズ/メモリ圧迫する問題への対策。ブロッキングの確認ダイアログではなく、ハード上限＋非ブロッキング警告を採用。`constants.ts` に `MAX_INPUT_FILES = 200`、`fileUtils.ts` に純粋関数 `addUniqueFilesWithLimit`（重複除外後に上限で切り詰め `{ files, truncated }` を返す）、`directoryReader.ts` の `collectFilesFromEntries` に収集件数の早期打ち切り（`maxFiles` 引数、残余バジェットを再帰にも伝播）を追加。`FileUploadArea` の共通投入処理 `addFiles` を上限付き（`addUniqueFilesWithLimit(..., MAX_INPUT_FILES)`）に差し替え、フォルダドロップ経路は `collectFilesFromEntries(entries, MAX_INPUT_FILES + 1)` で有界化。超過時は `role="alert"` の警告ボックス（i18n `fileUpload.limitExceeded`、`{{max}}` 補間）を表示しリストクリアでリセット。単体テストは `fileUtils.test.ts` / `directoryReader.test.ts` に追加
- 変換ページのバッチ処理を Web Worker + OffscreenCanvas のワーカープールへ移し、`navigator.hardwareConcurrency` を上限に並列実行するようにした（Issue #32・#47）。AVIF の WASM エンコード（従来メインスレッドで同期実行し UI をフリーズさせていた）も Worker 内で実行する。Worker（`src/workers/`: `imageProcessing.worker.ts` / `imageProcessingPool.ts` / `messages.ts`）とメインスレッドで共有する型・純粋ロジックを Canvas 非依存の `conversionCore.ts`（コア型 + `searchQualityForTargetSize` / `calculateTargetSize`）・`concurrency.ts`（`mapWithConcurrency` / `resolveConcurrency`）・`pngQuality.ts`（`pngQualityStrategy`）・`conversionResult.ts`（`buildConversionResult`）・`decodedImage.ts` に切り出し。OffscreenCanvas 非対応環境や Worker 個別失敗時はメインスレッドの `convertImage` にフォールバック。出力・対応形式・オプション（EXIF 保持・目標ファイルサイズ・品質・リサイズ）の仕様は不変で crop / metadata は変更なし。単体テストは `concurrency.test.ts` / `pngQuality.test.ts`、実ブラウザ検証は `e2e/convert.spec.ts`（一括変換の ZIP 全件検証 / AVIF バッチ / Worker 生成確認）
- PWA 化（オフライン対応・インストール可能化）に対応（Issue #33）。静的エクスポート + Cloudflare Pages 構成に合わせ、ビルドツール非依存の「手書き SW テンプレート（`scripts/sw-template.js`）+ postbuild ジェネレーター（`scripts/generate-sw.ts`）」方式で Service Worker（`out/sw.js`）を生成。`src/app/manifest.ts`（Web App Manifest → `/manifest.webmanifest`）・`public/icons/`（192 / 512 / maskable）・`public/_headers`（sw.js は no-cache 等）・`ServiceWorkerRegister`（本番のみ登録）・`InstallPrompt`（A2HS 導線、i18n `install.*`）を追加。プリキャッシュ判定・URL 変換・キャッシュバージョン（FNV-1a）算出は純粋関数 `precache.ts` に切り出して単体テスト（`precache.test.ts`）。プリキャッシュ URL のハッシュをキャッシュ名に含め、`_next/static/**` が変わったときだけ更新。`skipWaiting` は付けず更新は次回起動時に反映。`postbuild` を `next-sitemap && node scripts/generate-sw.ts` に、`tsconfig.json` の `exclude` に `scripts` を追加。実ブラウザ検証は `e2e/pwa.spec.ts`（manifest / theme-color / 全ルートのオフライン描画）。合否は Lighthouse の PWA カテゴリ廃止（Lighthouse 12）のため DevTools Application パネルと pwa.spec.ts で担保
- 全ページ（convert / crop / metadata）の画像投入方法にクリップボード貼り付け（Ctrl/Cmd+V）とフォルダドロップ（サブフォルダを含む再帰取込）を追加（Issue #35）。共通フック `usePasteImages`（`src/hooks/`）と純粋ロジック `directoryReader.ts` を追加し、`FileUploadArea` の投入処理を共通関数 `addFiles`（`filterValidFiles` → `addUniqueFiles`）に集約。クリップボード取り込みは `fileUtils.ts` の `getFilesFromClipboardData`。非画像や各ページの受理形式外は既存の MIME フィルタで除外。単体テストは `directoryReader.test.ts` / `fileUtils.test.ts`、実ブラウザ検証は `e2e/paste-and-folder-drop.spec.ts`
- EXIF 対応を拡張（Issue #34）。(1) メタデータページで WebP（RIFF の EXIF チャンク）と PNG（eXIf チャンク）の EXIF 読み取りに対応（取り出した TIFF を合成 JPEG に包んで exif-js に渡す）、(2) 変換・トリミングの「EXIF を保持」を JPEG 以外の出力（PNG の eXIf チャンク / WebP の VP8X + EXIF チャンク）でも有効化（AVIF は非対応。convert の preserveExif 有効条件を「JPEG のみ」→「AVIF 以外」に変更）、(3) メタデータページに GPS 位置の処理モード選択（削除 / 市区町村レベルに約 1km 精度で丸める。JPEG のみ有効）を追加。バイナリ操作を純粋関数 `exifBinary.ts`（単体テスト `exifBinary.test.ts`）に、ブラウザ側の読み取り〜書き込みの橋渡しを `exifTransfer.ts` に切り出し。GPS 十進変換・丸めは `metadataManager.ts` に純粋関数として追加。実ブラウザ検証は `e2e/metadata.spec.ts`（WebP / PNG 読み取り・GPS 丸め）・`e2e/convert.spec.ts`（JPEG→PNG / →WebP の EXIF 保持）
- 各ページ（`/`, `/convert`, `/crop`, `/metadata`）に固有の SEO メタデータ（title / description / OGP / Twitter card / canonical）を付与（Issue #27）。`pageMetadata.ts` の純粋関数 `buildPageMetadata` で組み立て、各ルートの `layout.tsx`（サーバーコンポーネント層）から export する。root の `layout.tsx` に `metadataBase`・title テンプレート・共通 description・OGP / Twitter 既定値を集約。主言語は日本語で静的 HTML に出力されるため i18n（ロケール JSON）は非経由。単体テストは `pageMetadata.test.ts`、実 HTML 出力の検証は `e2e/seo-metadata.spec.ts` で実施
- 変換ページに目標ファイルサイズ (KB) 指定を追加（Issue #30）。指定すると品質値を二分探索し目標サイズ以下で最大品質の結果を採用する（JPEG / WebP のみ。PNG は可逆・AVIF は WASM が低速なため対象外）。探索は Canvas 非依存の純粋関数 `searchQualityForTargetSize`（`imageConverter.ts`）に切り出して単体テスト、実サイズ検証は E2E で実施。達成不可時は最小サイズで出力し結果一覧に警告表示
- `/start-issue` コマンドを git worktree 対応に変更。Issue ごとに `.claude/worktrees/issue-{番号}/`（gitignore 済み）へ worktree を作成して作業するため、複数 Issue の並列作業が可能に
- 変換ページに TIFF 入力対応を追加（`utif2` による動的デコード、Issue #26）。crop / metadata の受理形式からブラウザで描画できない TIFF を除外し、変換失敗ファイルの一覧通知を追加
- Next.js を 16 に更新（PR #45）。Turbopack 本番ビルドが無限ハングする上流バグの回避のため `build` スクリプトを `next build --webpack` に暫定変更（dev は Turbopack のまま。上流修正後に戻す）
- 変換ページに HEIC/HEIF 入力対応を追加（libheif WASM による動的デコード、Issue #28）
- 画像フォーマット変換の出力形式に AVIF を追加（`@jsquash/avif` の WASM エンコーダーを動的 import で使用）
- 開発ハーネスを整備（詳細は `docs/HARNESS.md`）: vitest による単体テスト、Claude Code フック（自動フォーマット / 完了時チェック）、サブエージェント（planner / docs-sync / reviewer）、PR 自動レビューフロー、GitHub Actions CI
- `feat/exif-editor` ブランチで EXIF メタデータの選択的削除機能を実装
